# Status global — onde o projeto está

> Atualizado em: **2026-07-25** · Baseline: **v0.1.0** (tag git)
> Regra: toda sessão significativa atualiza este arquivo + cria entrada em `historico/`.
> **O que fazer em seguida está em `.context/roteiro.md`** (ordem acordada com o Daniel em 25/07).

## De onde veio (resumo em 4 linhas)

- **15/07/2026** — primeiro commit: "Release v0.1: lançamento inicial da plataforma" (Django + Next, LP dinâmica seed-first).
- **16/07** — reformulação de produção: nginx no host da VPS, portas loopback, split api SSR/browser, mídia relativa; links de avaliação por turma (compartilhado) além de individuais; fotos de formatura no convite.
- **17/07** — subsistema 09 completo (Acervo de Mídia + Studio integrado + Postagens) via orquestração de agentes; bugfix `[hidden]` CSS; upload múltiplo + detecção de duplicados.
- **18/07** — reorganização (protótipos → `mvp-apps/`), estrutura de memória (`.context/` + `specs/` + constituição) e **baseline v0.1.0**.
- **18/07 (tarde)** — Studio 2.0: correção de 4 desalinhamentos de UI/UX achados no teste real (postagens viram accordion + overflow; título da capa de reel `central` deixa de vazar; texto do educativo `erro-certo` centralizado; barra de variantes não fica mais sob os thumbnails). Ver `historico/2026-07-18-studio-2-0-fixes-ui.md`.
- **18/07 (noite)** — **Spec 008: acervo em camadas** (correção de rumo do turma-cêntrico): `Midia` com camadas turma/curso/instrutores/estrutura/externa/geral, postagens multi-contexto, Mesa de Luz + Studio DA MARCA, seletor de camada no picker (arte mistura camadas). Suíte 50/50. Ver `historico/2026-07-18-acervo-em-camadas.md`.
- **19/07 (tarde)** — Limpeza de repositório: `design-system-junto/` (duplicata) removida, arte bruta do laboratório do Studio (`mvp-apps/studio/montar-templates/formacao-turma/`, ~109MB) tirada do versionamento (fica local, `.gitignore`), histórico do git reescrito com `git filter-repo` p/ o `.git` emagrecer (~137MB → poucos MB). Ver `historico/2026-07-19-limpeza-repo-git.md`.
- **19/07** — **Spec 001 (suíte de testes) IMPLEMENTADA por completo, T1–T8**: 120 testes (era 45), `config/settings/test.py` isolado, `apps/nucleo/testing.py` (helpers), cobertura nova em `cursos`/`leads`/`avaliacoes`/`educacional`/`contas` (LP, leads, magic-link de avaliação e de carteirinha, JWT), expansão pesada de `midia` (EXIF, dedup completo, curadoria, consentimento, ZIP, 403 exaustivo, páginas staff da turma) e de `ia` (adapter Gemini mockado). Runner `plataforma/rodar-testes.sh` (+ `--full`). Achado: painéis de `cursos`/`leads`/`avaliacoes`/`nucleo` só aceitam JWT (não sessão) — registrado em `specs/001-suite-de-testes/tasks.md` §Log, nenhum código de produção alterado. T9 (CI) não disparada.
- **20/07** — **Studio ✨: campo de instrução livre para melhorar/encurtar/variações** (branch `feature/ia-instrucao-livre`). Achado do Daniel: o texto atual já ia certinho pro prompt (`texto_atual` sempre chegava em `apps/ia/prompts.py`), mas a ação de "melhorar" só mandava a palavra-chave fixa (`instrucao: "melhorar"`), sem espaço pro usuário guiar a reescrita. Agora, ao clicar Melhorar/Encurtar/3 variações no Studio, abre um campo opcional (`pedirDetalhe()` em `studio.js`) que vira `detalhe` no contexto — linha própria no prompt ("Detalhe pedido pelo usuário: ..."), "Tentar de novo" reaproveita o mesmo detalhe. `texto.gerar` (campo vazio) fica fora do escopo por ora. Suíte `apps.ia` 43/43 (1 teste novo). Ver `historico/2026-07-20-ia-instrucao-livre.md`.
- **21/07** — **Fix: agente MAG (SDR) quebrado em PRODUÇÃO (2 bugs em cadeia)**. (1) Gemini rejeitava as tools (`400 ... key cannot be empty`) porque 4 dos 5 nós `toolHttpRequest` em prod tinham `sendHeaders:true` com o campo de header errado, caindo no default vazio do node (header sem nome exposto ao modelo). (2) Ao remover o header por completo pra corrigir o #1, quebrou outra coisa: prod tem `SECURE_SSL_REDIRECT=True` (dev não tem) e sem `X-Forwarded-Proto` o Django redireciona as chamadas internas pra `https://magma-backend-interno:8000` — porta que só fala HTTP puro — e o n8n estourava SSL seguindo o redirect. Fix final: header de volta, mas como valor fixo `parametersHeaders.valueProvider:fieldValue` (não exposto ao modelo, não quebra o Gemini). Corrigido e **testado em prod com mensagem real** (tools + resposta OK). Commitado na VPS (`c54e1de`, depois `8c2efb5` com o fix definitivo, depois `84d0bb5`) — já mesclado na master local, nada pendente. Ver `historico/2026-07-21-fix-gemini-tools-prod.md`.
- **21/07 (tarde)** — **Spec 013: B1 Operadora (Fase 1, só leitura) ENTREGUE**. Agente novo "Operadora - Secretária Digital" no ramo gestor/instrutor do workflow `MAG - Fase 0` (antes só recebia um eco de teste) com 3 tools: `status_turma` e `gerar_link_avaliacao` (já existiam) + `listar_leads` (ação nova, `apps/leads/acoes.py`, filtra por dias corridos + status). Testado de ponta a ponta em dev via `n8n_test_workflow`/webhook direto, número do gestor: as 3 consultas funcionaram com dado real (uma mensagem chegou de verdade no WhatsApp), e um pedido de escrita foi recusado com honestidade. Achado de ambiente (não é bug): DNS do container `magma-n8n-dev` deu `SERVFAIL` pro Gemini na 1ª rodada — `docker restart magma-n8n-dev` resolveu (glitch transitório, mesmo erro também acontecia no SDR já existente, então não foi causado pela mudança). Suíte 44/44 (`apps.nucleo`+`apps.leads`). Ver `specs/013-agente-whatsapp-operadora-leitura/` e `historico/2026-07-21-b1-operadora-leitura.md`.
- **21/07 (noite)** — **Spec 013, adendo: matrículas + link de matrícula**. Daniel testou de verdade pelo WhatsApp e notou que o gestor não via quantas matrículas a turma já tinha nem gerava link de matrícula pelo chat. Achado: o model `Matricula` (`apps/educacional/`) **já existe** (mesmo padrão do `ConviteAvaliacao` — token UUID, escopo turma/individual, `.url` = `/carteirinha/{token}`), só não estava na Camada de Ações. Fix: `status_turma` ganhou `matriculas` (conta só `ativa`/`concluida`); ação nova `gerar_link_matricula` (`apps/educacional/acoes.py`, mesmo padrão de `gerar_link_avaliacao`); Operadora ganhou a 4ª tool. Também reorganizei visualmente o workflow n8n ("Tidy Up" nativo + reposicionar o `Gemini Chat Model` compartilhado pro meio das 2 pistas). **Bug cometido e corrigido no caminho**: `n8n_update_partial_workflow` com `updateNode` num campo aninhado (`parameters.options.systemMessage`) reconstruiu o objeto inteiro e apagou `promptType`/`text`/`maxIterations` do agente — sempre conferir com `mode: filtered` depois de editar campo aninhado. Testado com número real do gestor (turma mudou de código "08/2026" pra "026" no meio da sessão — a Operadora reagiu certo, pediu confirmação em vez de inventar). Suíte completa 169/169. Ver `specs/013-agente-whatsapp-operadora-leitura/tasks.md` (T7-T9).
- **21/07 (noite, antes de commitar)** — **Spec 013, adendo 2: `listar_turmas`**. Daniel pediu uma forma de listar as turmas (código, curso, status) pra quando não lembrar o código de cabeça. Ação nova `listar_turmas` (`apps/cursos/acoes.py`, sem parâmetro obrigatório, filtro opcional `status`). Operadora ganhou a 5ª tool, instruída a chamar `listar_turmas` proativamente (ex.: "quais turmas estão abertas?" → a IA sozinha filtrou por `status=inscricoes`, sem precisar de lógica hardcoded pro que é "aberta"). Suíte completa 170/170. Ver `specs/013-agente-whatsapp-operadora-leitura/tasks.md` (T10-T11).
- **21/07 (noite, antes de commitar)** — **Bugfix real na plataforma (achado do Daniel testando `gerar_link_matricula`)**: pediu o link de matrícula e recebeu o link de um aluno que já tinha preenchido a carteirinha, não um convite em aberto. Causa raiz: `MatriculaConvitePublicoView.post` (`apps/educacional/views.py`) criava a matrícula individual do aluno **sem** marcar `escopo=INDIVIDUAL` — herdava o default do model (`TURMA`), então colava no filtro de "convite reaproveitável". Bug pré-existente da plataforma (não introduzido pela spec 013), só ficou visível porque `gerar_link_matricula` foi a 1ª coisa a consultar esse campo pra decidir qual link reusar. Fix em 2 camadas: causa raiz (view marca `escopo=INDIVIDUAL` explicitamente) + defesa em profundidade (`gerar_link_matricula` também exige `preenchida_em__isnull=True`, nunca confia só no `escopo`). Dados já afetados no dev corrigidos via shell. Teste novo reproduz o cenário exato. Suíte completa 171/171. Ver `specs/013-agente-whatsapp-operadora-leitura/tasks.md` (T12).
- **21/07 (noite)** — **Evolution API passa a persistir mensagens/chats** (achado do Daniel: só via no Manager o que a MAG respondia, não o que o contato mandava). Causa: só `DATABASE_SAVE_DATA_INSTANCE` estava ligado. Adicionadas `DATABASE_SAVE_DATA_NEW_MESSAGE`/`DATABASE_SAVE_MESSAGE_UPDATE`/`DATABASE_SAVE_DATA_CONTACTS`/`DATABASE_SAVE_DATA_CHATS` em `plataforma/evolution/docker-compose.dev.yml` e `plataforma/docker-compose.prod.yml`. Dev: container recriado e confirmado ativo. **Prod pendente** (só arquivo, precisa aplicar na VPS). Ver `historico/2026-07-21-evolution-persistencia-mensagens.md`.
- **23/07** — **3 cursos novos no banco dev, a partir dos certificados em `certificados/`**: `bls-suporte-basico-de-vida` (20h), `primeiros-socorros-lei-lucas` (4h, Lei Lucas 13.722/2018) e `puncao-venosa-coleta-exames` (10h) — cada um com textos de venda, 5-6 habilidades e FAQs, instrutor o instrutor Bello (COREN-RJ 525874-ENF) vinculado. Ficaram `status=rascunho` (sem imagens ainda, sem revisão do Daniel). Ver `historico/2026-07-23-cursos-a-partir-de-certificados.md`.
- **23/07 (cont.)** — Fix key React duplicada na home ao publicar BLS (slug do card estático não batia com o slug real; ver `historico/2026-07-23-fix-key-duplicada-home-bls.md`).
- **23/07 (cont.)** — **Novo WhatsApp primário em toda a plataforma**: `(21) 97976-7821` (era o número do banner do workshop, dúvida documentada em `07c-social-maker-calendario-piloto.md`) agora é o `whatsapp_principal` oficial; `(21) 96494-6079` vira secundário; `(21) 97100-5197` sai de circulação. Trocado em `ConfiguracaoSite` (banco dev + migração), fallback do frontend, guia de marca/tokens, template do certificado (`design-system/`), Studio (`marca.js`/`studio.js`), prompts da IA e docs técnicos. Suíte 171 (backend) + 56 (frontend) OK. Ver `historico/2026-07-23-troca-whatsapp-principal.md`.
- **23/07 (noite)** — **Spec 014 (Aluno durável + matrícula pelo WhatsApp) ENTREGUE por completo** (T1-T14), a pedido do Daniel ("vamos terminar essa spec 014") — T1-T5/T7/T-adm já vinham prontos de sessão anterior, fechados T6 (frontend `/carteirinha/nova/[token]` + `/carteirinha/[token]` separados) e T8-T14 (testes faltantes, ações `buscar_aluno`/`matricular_aluno`, Operadora no n8n, teste real, docs). 192 testes backend (era 171) + typecheck/lint frontend limpos. Teste real via `n8n_test_workflow` achou e corrigiu 2 bugs: a IA inventava `aluno_token` quando a confirmação vinha em mensagem separada (corrigido no system prompt — sempre re-buscar antes de matricular) e uma corrupção pré-existente (não desta sessão) no node do SDR, com o system prompt inteiro zerado — recuperada a partir do JSON versionado. Ver `specs/014-aluno-duravel-matricula-whatsapp/tasks.md` §Log e `historico/2026-07-23-spec-014-entregue.md`.
- **23/07 (madrugada seguinte)** — **Spec 015 (integração financeira Asaas) ENTREGUE no backend (T1-T10)**, a pedido do Daniel ("quero começar a implementação da spec de pagamento, já criei a conta sandbox"). App novo `apps/financeiro/`: `ConfiguracaoAsaas` (credencial + token de webhook cifrados, reusa `apps.ia.crypto`), `Cobranca` (ligada à Matrícula), `EventoWebhookAsaas`; adapter Asaas (`requests` puro); ações do agente `gerar_cobranca`/`consultar_pagamento`; **1º webhook HTTP externo do projeto** (`POST /api/financeiro/webhook/asaas/`, auth por header `asaas-access-token`); Admin com credencial mascarada + criação manual de fallback. 226 testes backend (era 192, +34). **Testado de ponta a ponta com credencial SANDBOX real**: túnel ngrok expondo o dev local (Asaas não alcança `localhost`), cobrança real criada (boleto — Pix indisponível nessa conta sandbox), pagamento simulado via API do próprio Asaas, webhook chegou de verdade e a `Cobranca` virou "paga" sozinha. Achado no caminho: bug pego pelo teste do Admin (`save_m2m` ausente no form customizado) antes de ir pra produção. Ver `specs/015-integracao-financeira-asaas/tasks.md` §Log e `historico/2026-07-23-spec-015-financeiro-asaas.md`. ~~**Pendente**: cadastrar `ConfiguracaoAsaas` de produção~~ — **resolvido**: o Daniel informou em 25/07 que a cobrança real já está configurada em produção.
- **23/07 (continuação, mesmo dia)** — **Spec 015 integrada no agente MAG via WhatsApp de verdade**, a pedido do Daniel ("quero agora integrar no agente, para criar os link de pagamento"). n8n dev precisou subir do zero (container nem existia, rede `magma-dev-net` recriada); 2° `runserver` em `:8000` (mesmo banco) só pra bater com o hostname `magma-backend-interno:8000` que os 26+ nós já usam (evita editar nó por nó). Operadora ganhou 2 tools novas (`gerar_cobranca`/`consultar_pagamento`, clonadas do padrão de `matricular_aluno`) + "PROTOCOLO DE COBRANÇA" no system prompt (via `patchNodeField`, nunca `updateNode` bruto — lição da spec 013). `TokenAgente` de dev ganhou os 2 escopos novos. **Teste real via `n8n_test_workflow`** (payload do Evolution, 3 mensagens): "o Fernando já pagou a 026?" respondeu com as 2 cobranças reais do T9; "gera uma cobrança de 20 reais..." fez a Operadora **parar e pedir confirmação** mesmo com todo dado já na mensagem; só depois do "sim, confirma" ela buscou o aluno de novo (token fresco) e gerou uma cobrança real nova no sandbox. Único erro nas 3 execuções foi esperado (Evolution API não roda nesta sessão). `plataforma/n8n/workflows/mag-fase-0-sdr.json` reexportado (30 nós) + `README.md` (checklist de prod) atualizados. Ver `historico/2026-07-23-spec-015-financeiro-asaas.md` §Continuação.
- **23/07 (madrugada seguinte)** — **Protótipo: buffer de mensagens fragmentadas do WhatsApp** (padrão "debounce", a pedido do Daniel depois de ver isso mencionado na internet). Container Redis novo e dedicado (`magma-n8n-redis-dev`, não reaproveita o Redis da Evolution API) + 9 nós novos no workflow `MAG - Fase 0 (eco WhatsApp)` entre `Extrair dados` e `Identificar Contato`: guarda cada mensagem numa lista Redis por número, espera 10s de inatividade, e só processa quando a própria execução confirma que é a "última vista" (senão aborta, a mais nova assume). **Testado de ponta a ponta com o exemplo real do Daniel** (`"18714933748"` + `"650"` em mensagens separadas ~2s) — confirmado que as duas viram uma mensagem só (`"18714933748 650"`) antes de chegar no agente SDR/Operadora. Achado incidental (não corrigido, pré-existente): agente SDR pode terminar em handoff sem gerar texto final, quebrando o `Responder no WhatsApp` com "Text is required". Ver `historico/2026-07-23-buffer-mensagens-whatsapp-prototipo.md` e ADR em `.context/decisoes.md`. Janela ajustada pra 5s a pedido do Daniel, commitado (`9432549`) e formalizado como **spec 016** em 24/07. **Só dev** — pendente teste com mensagem real via Evolution (T9) e promover pra prod (T10), ver `specs/016-buffer-mensagens-fragmentadas/tasks.md`.
- **24/07** — **Spec 017 (info institucional da SDR) ENTREGUE em dev**, a partir de uma análise das ferramentas do n8n pedida pelo Daniel (gap já registrado nas "lacunas conhecidas" abaixo). Ação nova `info_institucional` (`apps/nucleo/acoes_institucional.py`) devolve endereço/WhatsApp/Instagram/e-mail sempre, e nota do Google/total de alunos formados só quando o respectivo toggle (`exibir_nota_google`/`exibir_total_formados`) está ligado — a ação filtra na origem em vez de confiar no agente pra "esconder" o dado, diferente do serializer público que expõe os campos crus pro front decidir. SDR ganhou essa 6ª tool (workflow `MAG - Fase 0`, 40 nós agora). 4 testes novos, suíte completa 230/230. **Achado de ambiente**: a porta 8000 do host estava ocupada por outro projeto (não a Magma, processo `root` alheio a esta sessão) — sem mexer nele, testei com um `runserver` de dev em `:8001` e os 2 nós envolvidos apontando pra lá temporariamente, revertido pra `:8000` (padrão) antes de reexportar o JSON. **Achado real**: a tool nova voltava 403 até o `TokenAgente agente-recepcionista-mag` (dev) ganhar o escopo `nucleo:info_institucional` (adicionado via shell). Testado via `n8n_test_workflow`: SDR respondeu endereço+Instagram reais sem inventar e sem citar nota/total (toggles desligados, como esperado); único erro foi o de sempre (Evolution API não roda nesta sessão). Ver `specs/017-agente-whatsapp-info-institucional/tasks.md` e `historico/2026-07-24-spec-017-info-institucional.md`. **Pendente**: promover pra prod (escopo novo no `TokenAgente` de prod + `promover-prod.sh`).
- **24/07 (continuação)** — **Spec 019 (B5 Radar — resumo diário) ENTREGUE em dev**, segunda opção da mesma análise de ferramentas do n8n (a primeira foi a 017; a Nutridora T+1/3/7 ficou de fora por decisão do Daniel, adiada). Nota de numeração: nasceu como "spec 018", mas colidiu com `specs/018-rastreamento-trafego-pago` criada em paralelo (outra sessão, plano de tráfego pago) — renumerada pra **019** antes de fechar. Ação nova `resumo_diario` (`apps/nucleo/acoes_resumo.py`) agrega leads das últimas 24h, turmas com inscrições abertas (código+vagas), avaliações pendentes, postagens agendadas pra hoje e uso de IA do mês — 5 testes novos, suíte completa 235/235. Workflow novo `MAG - Radar (resumo diário)` (id `kq6ULUF5lYU9HRQf`, 4 nós: `Schedule Trigger` 8h America/Sao_Paulo → `HTTP Request` → `Code` formata texto → `HTTP Request` Evolution) — **sem AI Agent**: é relatório factual, formatado por código determinístico, não por LLM (decisão registrada no `plan.md`: dado exato não deveria passar por geração). Escopo `nucleo:resumo_diario` no `TokenAgente` de dev. Mesmo achado de ambiente da spec 017 (porta 8000 do host ocupada por outro projeto, testado em `:8001` e revertido pra `:8000`). Como `Schedule Trigger` não dá pra testar via `n8n_test_workflow`, usei um webhook temporário só pra disparar manualmente e removi antes de exportar. **Teste real**: resumo saiu com dado de dev de verdade (3 leads em 24h, turma "026" aberta, 0 avaliações pendentes, 18 execuções de IA/14060 tokens no mês); único erro foi o de sempre (Evolution API não roda nesta sessão). Workflow **ativado** em dev (cron real passa a disparar 8h a partir de agora). Ver `specs/019-agente-whatsapp-radar-diario/tasks.md` e `historico/2026-07-24-spec-019-radar-diario.md`. **Pendente**: promover pra prod (workflow ainda não em `ids-prod.json` — primeira promoção — + escopo novo no `TokenAgente` de prod + ativar depois de importar).
- **24/07 (continuação)** — **Spec 020 (Nutridora T+1/3/7) ENTREGUE em dev**, a pedido do Daniel pensando na campanha de tráfego pago (spec 018) que pode começar amanhã e trazer leads reais pelo WhatsApp. `Lead` ganha `nutridora_ultimo_toque` (t1/t3/t7, migração aplicada em dev); ação nova `processar_nutridora` (`apps/leads/acoes.py`) busca leads elegíveis por janela (T+1 ≥1 dia, T+3 ≥3 dias e já em t1, T+7 ≥7 dias e já em t3), exclui `whatsapp` vazio, `utm_source=whatsapp` (lead nascido de conversa ao vivo) e números em `ContatoEscalado` (handoff — mesmo mecanismo de silêncio, sem opt-out novo), monta o texto com **dado real** (T+1: habilidades reais do curso; T+3: avaliação aprovada real — sem inventar, se não houver nenhuma ainda **não avança** e tenta de novo no dia seguinte; T+7: vagas restantes reais se `exibir_vagas`, senão texto genérico), marca o toque e devolve a lista pra mandar — no máximo 1 toque por lead por execução mesmo se atrasado em vários estágios. 11 testes novos, suíte completa 245/245. Workflow novo `MAG - Nutridora (T+1/3/7)` (id `ZkAxwOPuWVxWncax`, 4 nós: `Schedule Trigger` 9h → `HTTP Request` → **`Split Out`** (node novo no projeto — transforma o array de leads em N itens) → `HTTP Request` Evolution, um envio por item automaticamente). Mesmos achados de ambiente das specs 017/019 (porta 8000 ocupada por outro projeto, testado em `:8001`/webhook temporário, revertido antes de exportar). **Teste real progressivo** (mesmo lead de teste, `criado_em` retroativo em 3 rodadas): T+1 puxou as 3 habilidades reais do Socorrista APH; T+3 puxou uma avaliação aprovada real (5 estrelas) do banco de dev; T+7 calculou vagas reais (capacidade 15, "restam 14 vaga(s)"). Escopo `leads:processar_nutridora` no `TokenAgente` de dev. Workflow **ativado** em dev. Ver `specs/020-agente-whatsapp-nutridora-t1-t3-t7/tasks.md` e `historico/2026-07-24-spec-020-nutridora-t1-t3-t7.md`. **Pendente, prioridade alta**: promover pra prod (workflow novo, primeira promoção + escopo no `TokenAgente` de prod) — diferente das specs anteriores, aqui a urgência é real por causa da campanha.
- **24/07 (continuação)** — **Adendo spec 019 (T9): Radar manda pra todos os gestores**. Daniel avisou que em produção vão ter 2 gestores e pediu pra buscar a lista no backend em vez do número fixo hardcoded. Ação nova `listar_gestores` (`apps/nucleo/acoes_gestores.py`, escopo `nucleo:listar_gestores`) — gestores ativos com WhatsApp cadastrado. Workflow do Radar ganhou 2 nós (`HTTP Request` busca gestores → `Split Out` separa por gestor, um envio por pessoa, mesmo texto do resumo pra todos). 4 testes novos, suíte completa 249/249. **Teste real**: a Evolution API estava rodando de verdade nesta sessão (subida em paralelo pelo Daniel) — a mensagem chegou de fato no WhatsApp real (`status: PENDING`, confirmado pela própria Evolution). Achado no caminho: um segundo gestor de teste com número fake foi corretamente recusado pela própria Evolution (`exists: false`) antes de eu remover o número de teste. **Lacuna registrada, fora de escopo deste adendo**: `avisar_equipe` (handoff, spec 012) ainda usa número fixo — mesma limitação, não corrigida agora porque não foi pedido.
- **24/07 (continuação)** — **Adendo spec 012 (T7): `avisar_equipe` também passa a mandar pra todos os gestores**, a pedido do Daniel logo em seguida. Diferente do Radar, `avisar_equipe` é chamada de dentro do raciocínio do AI Agent (uma tool, não um pipeline linear), então a correção não podia ser só "trocar por HTTP Request comum". Solução: sub-workflow novo `MAG - Avisar Equipe` (`mag-avisar-equipe.json`, webhook próprio) — recebe `{mensagem}`, chama `listar_gestores`, separa com `Split Out`, manda pra cada um via Evolution. A tool `avisar_equipe` do SDR só trocou a `url` (de `evolution-api` direto pra `http://localhost:5678/webhook/avisar-equipe` — o n8n chamando a si mesmo, portável entre dev/prod sem config nenhuma) e o `jsonBody`; prompt do SDR não mudou. **Teste real completo via handoff de verdade**: mensagem simulando "quero pagar agora" → SDR chamou `escalar_contato` + `avisar_equipe` na ordem certa → aviso chegou de verdade no WhatsApp (Evolution rodando de verdade nesta sessão) → resposta final ao lead também confirmada. Suíte completa 249/249. Ver `specs/012-agente-whatsapp-handoff/tasks.md` (T7).
- **24/07 (noite)** — **Deploy completo pra produção** — a pedido do Daniel, pra fechar a sessão ("commitando, dando push, e atualizando na vps já com tudo"). `git push` + `git pull` na VPS (6 commits, `f7939fe`→`0109c5b` então `4595c03`). Backend: `docker compose build backend` (código não é bind-mount, precisa rebuild) + `up -d` — **incidente no meio do caminho**: rodei `docker compose up -d backend` sem `--env-file .env.prod` (o comando certo, usado pelo `init-prod.sh`), o que fez o Compose substituir `${MYSQL_USER}`/`${MYSQL_PASSWORD}`/etc. por vazio e recriar `db`+`backend` com credenciais em branco — site ficou fora do ar (502) por ~1-2 min até eu perceber pelo log do backend (`Access denied for user 'root'@'...' (using password: NO)`) e rodar de novo com `--env-file .env.prod` certo. Dado confirmado intacto depois (4 cursos, 2 turmas, 4 usuários — nada perdido, só a config de conexão estava errada). Migrations (inclusive `leads.0002_lead_nutridora_ultimo_toque`) aplicadas automaticamente pelo `entrypoint.sh` no boot. `TokenAgente` de prod (`agente-n8n-mag`) ganhou os 4 escopos novos (`nucleo:info_institucional`, `nucleo:resumo_diario`, `leads:processar_nutridora`, `nucleo:listar_gestores`). n8n: os 3 workflows novos (`MAG - Avisar Equipe`, `MAG - Radar`, `MAG - Nutridora T+1/3/7`) foram importados pela 1ª vez via `n8n import:workflow`/`update:workflow --active=true` (CLI, não tem UI headless) — achado no caminho: import exige um `id` no JSON mesmo pra workflow novo (gerado localmente, formato nanoid 16 chars) e `--activeState=fromJson` não funciona nesse modo de deployment (não-cluster), por isso ativação em 2 passos. **Bloqueio real**: `mag-fase-0-sdr.json` já carregava os nós do buffer (spec 016, nunca promovido — T10 pendente) e a credencial Redis não existia em prod; perguntei ao Daniel (`AskUserQuestion`) se subia o buffer junto ou adiava o SDR — escolheu subir junto. Redis dedicado (`n8n-redis`) adicionado ao `docker-compose.prod.yml` (commit `4595c03`) e credencial criada **manualmente pelo Daniel** no editor de prod (criação de credencial sempre foi passo manual documentado no projeto, não dá pra automatizar com segurança sem replicar a criptografia interna do n8n). Com a credencial pronta, `promover-prod.sh mag-fase-0-sdr.json` (atualização normal, já mapeada) — importa, publica, reinicia o n8n uma vez só (consolidando a ativação dos 3 workflows novos + a atualização do SDR no mesmo restart). **Resultado**: os 5 workflows do agente MAG ativos em prod (`MAG - Fase 0`, `MAG - Nutridora T+0`, `MAG - Avisar Equipe`, `MAG - Radar`, `MAG - Nutridora T+1/3/7`), todos os containers saudáveis, `ids-prod.json` atualizado com os 3 IDs novos de workflow + o ID da credencial Redis. Não foi disparada nenhuma mensagem sintética de teste em prod (evitar Lead/custo de IA fake no banco real) — checagem final foi containers saudáveis + execuções recentes sem erro.
- **24/07 (noite, depois do deploy)** — **Specs 021 (registro de conversas) e 022 (memória no Redis) ENTREGUES em dev**, a pedido do Daniel ("preciso gravar as sessões de conversas do agente... hoje eu tenho que olhar as execuções, e não dá pra fazer análise mais ampla, nem uma LLM analisar"). **021**: app novo `apps/conversas/` (`Conversa` + `Turno`, sessão delimitada por 6h de inatividade, `desfecho` derivado das ferramentas por código — `registrar_lead`→lead, `escalar_contato`→handoff, `matricular_aluno`→matrícula, `gerar_cobranca`→cobrança, sempre o ponto mais avançado); 3 ações (`registrar_turnos`, `exportar_conversas` com transcrição em texto corrido pra LLM ler em bloco, `purgar_conversas`); retenção em `ConfiguracaoSite.conversas_retencao_dias` (**padrão 15, `0` = nunca**, editável no Admin sem redeploy — pedido explícito do Daniel de "só mudar uma config"); Admin somente leitura. No n8n: 1 nó de registro por pista **depois** do envio (aproveitando o `returnIntermediateSteps` que os 2 agentes já tinham ligado — grava texto + ferramentas de uma vez, sem custar latência), 1 em cada Nutridora (papel `sistema`), 1 de purga no fim do Radar, todos com `onError: continueRegularOutput`. **022**: os 2 nós `memoryBufferWindow` (RAM, morriam a cada restart do n8n) viraram `memoryRedisChat` com `sessionTTL` 6h, reusando o Redis e a credencial do buffer da spec 016 — que **já existia em prod**, então a promoção não ganha nenhum passo manual. 29 testes novos, suíte completa **278/278**. **Testes reais**: conversa de 2 mensagens virou 1 conversa com 4 turnos e desfecho `handoff`; purga apagou só a vencida (40 dias) e preservou a recente; e a memória **sobreviveu a `docker restart magma-n8n-dev`** — a MAG lembrou "setembro" e "segurança em eventos" depois do restart, o que era impossível antes. **Achados**: (1) bug no código novo pego por teste — `params.get("dias") or 7` transformava `dias=0` em 7 em vez de recusar; (2) com `sessionIdType: customKey` a chave do Redis é literalmente a expression, então SDR e Operadora **compartilhariam memória** (regressão silenciosa vs. Simple Memory) — corrigido prefixando `mag:sdr:`/`mag:operadora:`; (3) reexportar workflows gerava ~1200 linhas de diff cosmético — virou script versionado `exportar-dev.sh` (contrapartida do `promover-prod.sh`), com o diff conferido semanticamente nó a nó. Ver `specs/021-registro-conversas-agente/`, `specs/022-memoria-conversa-redis/` e `historico/2026-07-24-specs-021-022-conversas-e-memoria.md`. **Pendente**: promover as duas juntas pra prod (mesmo `mag-fase-0-sdr.json`, um restart só) — escopos `conversas:*` no `TokenAgente` de prod + migrações + `promover-prod.sh`.
- **24/07 (madrugada seguinte)** — **Spec 023 (correções da SDR) ENTREGUE em dev** — primeira spec nascida **de uma análise de conversa real**, que é exatamente o que a spec 021 foi criada pra viabilizar. O Daniel testou a MAG pelo WhatsApp, pediu a análise da conversa, e saíram **5 problemas** que nenhuma execução isolada do n8n mostraria (todas terminaram em "sucesso"): (1) 🔴 a MAG **prometeu chamar um consultor e não chamou** — só `registrar_lead`, sem `escalar_contato`/`avisar_equipe`: lead esperando alguém que nunca foi avisado, e a MAG nem silenciada; era **intermitente** (no teste anterior do mesmo dia tinha funcionado); (2) 🔴 **lead duplicado** (2 `Lead` pro mesmo número); (3) 🟠 **lead sem curso**; (4) 🟠 **"ou prefere ver outras datas?"** sem existir outra data — achado do Daniel: a pergunta fabricou a objeção, desmontou a escassez que a própria MAG tinha criado, e o lead pegou a deixa; (5) 🟠 **"já registrei seu interesse no sistema"** — 2º achado do Daniel: "o cliente não precisa saber que ele é um lead". Correções em duas camadas: **backend** (dedup por WhatsApp + `resolver_curso()` por semelhança) e **prompt/tool do SDR** (5 regras novas + descrições nos campos da tool). **Achado maior no meio do caminho**: o `curso` vazio **não era problema de prompt** — a MAG *nunca acertou o slug* (`socorrista-aph-120h`, `aph-120h`, ...), sempre montando a partir do nome exibido, e o backend engolia em silêncio; virou `resolver_curso()` com pontuação por termos e `logger.warning`. **Testes reais**: handoff **3 de 3** com três formulações diferentes de fechamento; lead único com `curso='socorrista-aph'`; fechamento sem oferecer data inexistente; pedido de nome sem narrar cadastro. Suíte **289/289** (era 278). **Incidente meu, registrado**: import editado num passo separado da função deixou o `runserver` com módulo velho e o endpoint respondendo `NameError` **enquanto a suíte passava** — teste verde não garante que o processo em execução tem o código novo. **Aberto pra decisão do Daniel**: no handoff sem nome, a MAG escala sem registrar lead — o contato mais quente fica fora da tabela de leads (só em `ContatoEscalado`+`Conversa`), sumindo do Radar e da Nutridora. Ver `specs/023-correcoes-sdr-handoff-e-lead/`. **Pendente**: promover 021+022+023 juntas pra prod.
- **24/07 (continuação)** — **Plano de tráfego pago (Meta Ads) refeito e documentado**, a pedido do Daniel (já tinha assinatura da Manus, pediu um guia lá — `magma_meta_ads_guide.md` — e trouxe pra análise). O guia original acertava a parte de marca mas assumia Pixel/CAPI que não existem, cronograma de 5+ semanas incompatível com os 15 dias até 08/08, orçamento em USD genérico e não considerava que o funil real é WhatsApp-first (MAG). Refeito com as restrições reais batidas com o Daniel: **só aquisição** (nada de retenção/reativação agora), **teto R$1.000** (reinveste conforme capta), só Socorrista APH. Campanha única, objetivo Mensagens (Click-to-WhatsApp, não depende de pixel), 1 conjunto, R$40-50/dia pra começar. Ver `docs/subsistemas/01b-trafego-pago-meta-ads.md` (plano completo) e `specs/018-rastreamento-trafego-pago/` (pixel client-side mínimo, PENDENTE — ainda não implementado, aguardando o Daniel decidir lançar).
- **24/07 (continuação)** — **Spec 018 (pixel de rastreamento) T1-T4 implementadas**, a pedido do Daniel ("quero ir passo a passo pra implementar o plano de tráfego pago"). Antes de tocar em código, conferido que specs 021/022 (outro agente rodando em paralelo, backend puro — `apps/conversas/`, `apps/nucleo/`) não têm nenhuma sobreposição de arquivo com o pixel (frontend puro). `app/layout.tsx` ganhou o script base do Meta Pixel (`next/script`, condicional a `NEXT_PUBLIC_META_PIXEL_ID` — sem a env, o site funciona normal e nada carrega); `WaLinks.tsx` dispara `Contact` no clique de qualquer link de WhatsApp; `LeadForm.tsx` dispara `Lead` só no caminho feliz do formulário (POST OK). Typecheck/lint limpos. **T5 (confirmar disparo com a extensão Meta Pixel Helper) fica pro Daniel** — precisa de um Pixel ID de teste real e do Chrome dele; passo a passo em `historico/2026-07-24-spec-018-pixel-implementado.md`. Próximo passo natural: criar o Pixel de verdade no Business Manager e montar a campanha em si (`docs/subsistemas/01b-trafego-pago-meta-ads.md` §6).
- **25/07 (madrugada)** — **Bateria de 6 conversas simuladas contra a SDR em dev** (5 perfis pedidos pelo Daniel + 1 controle), cada um em número próprio (prefixo `55009000000X`, DDD 00 inexistente — nada foi entregue a WhatsApp real). **Nada foi alterado: sessão só de observação.** Resultado principal: **6 de 6 conversas terminaram em handoff** e **3 de 6 não geraram lead** — inclusive o contato mais quente da amostra ("quero garantir minha vaga", nome dado, curso identificado), o que confirma na prática a questão que a spec 023 deixou aberta pra decisão do Daniel. Outros achados: a MAG escalou perguntas que ela mesma sabia responder pelo FAQ que já tinha em mãos (parcelamento), e respondeu a **mesma** pergunta de 3 formas diferentes em 3 conversas; `ContatoEscalado` silencia o contato **pra sempre** (o que reclamou de não ter resposta perguntou o preço depois e levou silêncio); a regra 6 da spec 023 vazou de novo em roupagem nova ("pra deixar seu cadastro certinho aqui"); `listar_cursos`/`detalhes_curso` foram rechamados até 4× pro mesmo curso na mesma conversa (regra 8 ignorada — suspeita: `contextWindowLength: 10`); e a MAG prometeu apurar um calendário de turmas futuras que nenhuma tool consegue responder. **Incidente de infra**: o n8n dev **travou inteiro** no meio do teste (processo principal a 100% de CPU, `healthz` fora, webhook parou de aceitar mensagem, >10min até `restart`) — em prod seria o atendimento fora do ar sem alarme; precisa de investigação própria. **O que passou bem**: zero invenção de dado de curso (tudo conferido contra a API), curso inexistente negado sem enrolar, honestidade sobre ser IA, nenhuma promessa de emprego mesmo sob apelo emocional, handoff completo (`escalar_contato`+`avisar_equipe`) em 6/6 — o 🔴1 da spec 023 não reapareceu —, buffer da spec 016 impecável, e `resolver_curso()`+dedup da spec 023 validados em campo (2 de 3 slugs vieram errados e foram corrigidos; `registrar_lead` rodou 2× pra mesma pessoa e gerou 1 lead só). Análise completa com as 6 transcrições e 9 correções propostas: `historico/2026-07-25-simulacoes-sdr-6-perfis.md`. **Resíduo em dev**: leads `#29`-`#31` e `ContatoEscalado` `#9`-`#14` (esses números seguem silenciados até serem apagados).
- **25/07 (continuação)** — **Daniel revisou a análise, aprovou as 9 correções e acrescentou as dele; virou 4 specs novas (024-027), todas PENDENTES**. Ele **corrigiu uma conclusão minha**: eu tinha marcado como falso positivo a escalada do lead que disse "650 tá salgado" — *"é muito importante que o gestor tome a frente quando o cliente não aceita o preço sugerido pela empresa"*. Com isso, 4 das 6 escaladas estavam **certas**, e o diagnóstico de fundo muda de "a MAG escala demais" pra **"a MAG escala certo, mas o handoff não tem rede embaixo"**. Achados novos dele lendo as transcrições: escassez irrelevante ("temos apenas 14 vagas" numa turma de 15 — *"eu mesmo iria achar até chato"*, só citar com ≤3), ficha técnica no lugar de convite (*"gostaria de conhecer melhor esse curso?"* + **foto/vídeo escolhido a dedo pelos gestores** — virou feature nova), curso inexistente fechando a porta em vez de citar os outros cursos, e o aviso de handoff ter que mandar o gestor responder **pelo WhatsApp da empresa** (*"a pessoa não tem que ter 2 vias de comunicação"*). E o princípio que passa a reger as correções: *"quanto mais regra colocamos, mais engessados deixamos... apenas mexa alguns pesos, porque na grande maioria do tempo ele foi cirúrgico"* — daí o **orçamento fixo de regras** da 024 (entra uma, sai uma). As specs: **024** (pesos de tom, escassez e curiosidade — só prompt + 2 nós, sem backend), **025** (handoff de ponta a ponta: lead garantido no `escalar_contato`, `ContatoEscalado` com estado/expiração, resposta de cortesia, canal único no aviso), **026** (mídia curada no atendimento — tag nova em `Midia.tags` reusando a Mesa de Luz; a IA decide *quando* oferecer, nunca *o que* existe), **027** (resiliência do n8n — T1 é investigação do travamento, com as blindagens entrando independentemente da causa). **Ordem obrigatória: 024 → 025 → 026** (as três editam o mesmo `systemMessage`; fora de ordem uma sobrescreve a outra). **Bloqueios de decisão do Daniel**: prazo de expiração do handoff e status do lead escalado (spec 025), tag nova × reusar `destaque` e se vídeo entra (spec 026). 7 ADRs novos em `.context/decisoes.md`. **Nada implementado — sessão de análise e documentação.**
- **24/07 (continuação)** — **Cartão-postal comercial** (`docs/comercial/cartao-postal-negocio-digital.html`), a pedido do Daniel: documento de venda do serviço de "operação digital" pra outros negócios locais (renda extra), usando empresa fictícia ("Doce Ofício", confeitaria) no mesmo cenário de partida da Magma, sem linguagem técnica — cada momento da narrativa mapeia uma entrega real (Radar, SDR, Nutridora, handoff, matrícula+cobrança). Publicado como Artifact privado. Não toca em código da plataforma. Ver `historico/2026-07-24-cartao-postal-comercial.md`.
- **25/07 (manhã)** — **Sessão de análise e ordenação: nasce o `.context/roteiro.md`**. O Daniel pediu uma leitura do estado e uma recomendação de por onde começar, e depois fixou a ordem: **T20 → produção → 026/028 → investigar o travamento → pixel/campanha**. Nada de código foi alterado. **Achado principal**: a T20 é maior do que estava registrada — o `registrar_lead` do SDR carimba `utm_source="whatsapp"` como valor fixo do workflow (`mag-fase-0-sdr.json`, nó `tool-registrar-lead`), e `processar_nutridora` exclui exatamente esse valor, então **nenhum** lead nascido de conversa com a MAG entra na régua T+1/3/7 — não só o caso da spec 028. Como a campanha do Meta é Click-to-WhatsApp, 100% do lead pago cairia nessa faixa morta, com a Nutridora ativa em prod desde 24/07 nutrindo só lead de formulário. **Decisão do Daniel**: trocar origem por atividade (exclusão por conversa recente via `apps.conversas`, possível só depois da spec 021), descartando a exceção estreita. **Correção de registro**: a cobrança real do Asaas **já está configurada em produção** (informado por ele; este arquivo afirmava o contrário desde 23/07). **Segundo achado, de processo**: as specs 021–025 + 027 parcial + pixel 018 T1–T4 continuam **fora do git** — 2.207 linhas em 26 arquivos modificados + 26 caminhos não rastreados, nada commitado desde `b90fc95` (24/07) —, enquanto a produção segue servindo o SDR de antes da 023/024/025 (quem é escalado não vira lead e fica silenciado até liberação manual). Daí a etapa 2 do roteiro. Ver `historico/2026-07-25-roteiro-execucao.md`.
- **25/07 (continuação)** — **Etapa 1 do roteiro (028-T20) ENTREGUE em dev**: a Nutridora passa a excluir por **atividade**, não por origem. `ConfiguracaoSite.nutridora_silencio_dias` (padrão **2**, migração `nucleo/0008`) + `Conversa.numeros_ativos_desde(dias)` (contrapartida de `ContatoEscalado.numeros_ativos()`, `0` = não checar) + `processar_nutridora` trocando `.exclude(utm_source="whatsapp")` por `.exclude(whatsapp__in=numeros_em_conversa)`. Suíte **300 → 304** (o teste que afirmava o comportamento antigo virou 5 que afirmam o novo). **Medição que dimensiona o furo**: os **12 leads do banco de dev têm `utm_source="whatsapp"`** — a régua antiga nutriria zero. Rodada real da ação com o lead #32 retro-datado em 3 dias: sem conversa recente **entra** (texto T+1 com habilidades reais do curso), com conversa de agora **não entra**; estado do lead restaurado depois. `docs/plataforma/03-api-contratos.md` atualizado. **Só vale em produção depois da etapa 2** — sem `apps.conversas` (spec 021) lá, o filtro novo não excluiria ninguém e todo lead de WhatsApp receberia toque, inclusive quem está no meio do papo. Ver `.context/roteiro.md` §Etapa 1.
- **25/07 (tarde)** — **Etapa 2 do roteiro ENTREGUE: specs 021-025 + 028-T20 + blindagens da 027 estão EM PRODUÇÃO**. 5 commits (`d82c09d`→`438e1b5`), push e `git pull` na VPS. Backend rebuildado com `--env-file .env.prod` nos dois comandos — `db` ficou `Running`, o incidente de 24/07 não se repetiu; as 4 migrações (`conversas.0001`, `leads.0003`, `nucleo.0007`, `nucleo.0008`) subiram pelo `entrypoint.sh` e o dado ficou intacto (4 cursos, 2 turmas, 4 usuários, 1 lead). `TokenAgente agente-n8n-mag` foi de 16 pra 19 escopos (os 3 `conversas:*`). Os 5 workflows foram atualizados e continuam ativos — **desviei do `promover-prod.sh` de propósito**: ele reinicia o n8n a cada arquivo, então montei os 5 com `_montar_json_prod.py`, importei e publiquei os 5, e reiniciei uma vez só (janela de segundos em vez de cinco). **027-T6/T7 fechadas com número medido**: `n8n --version` em prod devolveu **2.31.4** — a hipótese que travava a tarefa estava invertida, prod está um patch *atrás* do dev (2.31.5), não à frente; fixado em 2.31.4 (o que já roda), e `mem_limit` de **1g** (metade do dev, porque a VPS tem 3 GB no total, ~1 GB livre, e o n8n usa 470 MiB). **Divergência dev 2.31.5 × prod 2.31.4 fica registrada como decisão à parte.** Verificado: site e API em 200, 8 containers de pé, n8n `healthy`, prune ligado. **Não verificado**: nenhuma mensagem real passou pela MAG depois do deploy (não disparei mensagem sintética em prod pra não criar Lead e custo de IA falsos) — falta o Daniel mandar uma mensagem de verdade e conferir se nasce `Conversa` no Admin. **Achado do deploy, fora de escopo**: o único `ContatoEscalado` de prod tem `numero` começando em `120363…`, que é **JID de grupo** do WhatsApp, não telefone — a MAG está processando mensagem de grupo e tratando o grupo como contato; com a 025 no ar, esse caminho passa a criar `Lead` com id de grupo no lugar do número.

- **25/07 (fim da tarde)** — **INCIDENTE ENCONTRADO E CORRIGIDO NO MESMO DIA: a MAG estava respondendo dentro de grupos de WhatsApp.** Apareceu ao conferir a produção depois do deploy (o único `ContatoEscalado` tinha `numero` `120363…`, que é JID de grupo). Investigado no Postgres da Evolution de prod: em **quatro grupos**, pelo WhatsApp principal do negócio (`<whatsapp-do-negocio>`) — `grupo de turma 1` e `grupo de turma 2` (ofereceu o curso a alunos **já matriculados nele**), `grupo pessoal do Daniel`, e `grupo de turma 3`, onde o instrutor postou o link de inscrição da turma e a MAG tratou o anúncio dele como lead: 4 mensagens na frente do grupo + acionou a equipe. **Causa**: `remoteJid.split('@')[0]` no nó `Extrair dados` — em grupo isso devolve o **id do grupo**, que passa por número sem levantar suspeita (só dígitos, sem pontuação). **Correção em duas camadas, as duas já em produção**: (1) backend (`84e173c`) com `numero_de_pessoa()` novo (`apps/nucleo/numeros.py`, só dígitos, 10-15 caracteres) aplicado em `identificar_contato` (400 em vez de "desconhecido" — é a 1ª ação de toda conversa, então a execução morre antes de qualquer texto sair), `escalar_contato`, `garantir_lead`/`validate_whatsapp`, `processar_nutridora` e `listar_gestores`; (2) n8n (`08f2a1c`) com a condição `remoteJid endsWith "@s.whatsapp.net"` — lista de **permissão**, então canal e status também já nascem de fora. **Validado**: no processo de produção em execução (os 3 ids de grupo reais recusados, o número da Magma aceito) e em dev com o regex temporariamente ampliado pra ACEITAR grupo — payload de grupo parou em **3 nós/116 ms**, payload individual rodou os **22 nós** com a SDR respondendo dado real. Suíte **304 → 315**. Base de prod limpa: zero `Lead`/`ContatoEscalado`/`Conversa` com número que não é de pessoa. **Decisão registrada**: a MAG ignora grupo por completo — ela não tem noção de "vários interlocutores", e atuar em grupo é feature própria, não remoção de filtro. Ver `historico/2026-07-25-mag-respondendo-grupos.md`.

## O que está PRONTO (v0.1.0)

| Frente | Entrega | Onde |
|---|---|---|
| Vitrine/captação | LP dinâmica seed-first (home + página de curso), leads, SEO (robots/sitemap/JsonLd) | `plataforma/frontend/` + apps `cursos`, `leads` |
| Avaliações | Magic-link individual e por turma, experiência migrada do protótipo, fotos priorizando acervo (capa primeiro, fallback FotoCurso) | app `avaliacoes` |
| Acervo + Studio + Postagem | Upload em massa c/ EXIF+thumbs+dedup (409/forcar), Mesa de Luz (curadoria D/C/A), Studio canvas (templates → artes 1080²), postagens c/ ZIP e legenda — tudo em `/dj-admin/` | app `midia` (T1–T5 DONE, smoke test completo) |
| Infra | dev (SQLite/`init-dev.sh`) e prod (VPS, nginx no host, `init-prod.sh`, MySQL) | `plataforma/` raiz |
| Marca | Design system v2 (Estrela da Vida), tokens W3C, guia p/ agentes | `design-system/` |
| Apps de apoio | Backend tem apps `contas`, `nucleo`; `educacional` já tem `Aluno` (identidade durável, carteirinha) + `Matrícula` pura + cadastro/card público + matrícula pelo WhatsApp (spec 014); `financeiro` (spec 015) gera cobrança real no Asaas (PIX/boleto/cartão) e recebe status por webhook — **ativo em produção desde 25/07** (credencial real cadastrada pelo Daniel) — gestão escolar completa ainda é futuro | `plataforma/backend/apps/` |
| Rede de segurança (testes) | **Spec 001 IMPLEMENTADA (2026-07-19)** — backend: 147 testes (`apps/<app>/tests.py`, Django-nativo), `config/settings/test.py`. Frontend: 56 testes (Vitest, `lib/*` + webhook `revalidate`, sem jsdom/RTL ainda). `plataforma/rodar-testes.sh` (+`--full`) roda os dois | `specs/001-suite-de-testes/`, `plataforma/rodar-testes.sh` |

## EM ANDAMENTO

- **Agente WhatsApp "MAG" — Fase 0 COMPLETA + Fase 1 em andamento (2026-07-20, branch `feature/evolution-api-compose`)**. Plano-mãe: `docs/subsistemas/02b-agente-whatsapp-n8n.md` (persona única + squad de subagentes n8n). As 7 decisões do §10 foram batidas pelo Daniel (ver `.context/decisoes.md`, 2026-07-20) — só a #6 (auth do upload de acervo) fica em aberto pra Fase 2.
  - **Infra** (`plataforma/evolution/`): Evolution API no compose dev+prod (Postgres+Redis dedicados, sem porta pública). Rede `magma-dev-net` liga n8n dev + Evolution dev (composes separados). MCP `n8n-mcp` configurado (escopo local) — Claude Code cria/edita/testa workflows direto.
  - **specs/009 a specs/013 — todas ENTREGUES e validadas com teste real** (não só automatizado): 009 identificação de contato (`identificar_contato`, `Usuario`→`Lead`→desconhecido); 010 SDR com IA (Gemini, 3 tools lendo API pública, lead registrado, nunca inventa preço/vaga); 011 Nutridora T+0 (boas-vindas automática ao criar lead, sem IA); 012 Handoff (`ContatoEscalado`, escala pro humano + silencia + libera no admin); 013 B1 Operadora leitura (gestor/instrutor consulta `status_turma`/`gerar_link_avaliacao`/`listar_leads` pelo chat, recusa escrita com honestidade).
  - **Spec 014 — Aluno durável + matrícula pelo WhatsApp, ENTREGUE (2026-07-23)**: `Aluno` vira identidade durável (token uuid, CPF unique/null, dono da carteirinha), `Matrícula` fica pura (aluno+turma+status, `unique(aluno,turma)`), acabou a Matrícula-fantasma (link de cadastro agora é `Turma.token_cadastro`, estável). Operadora ganha a 1ª ação de **escrita**: `buscar_aluno`+`matricular_aluno`, protocolo buscar→confirmar→matricular (nunca insere sem confirmação humana). Ver `specs/014-aluno-duravel-matricula-whatsapp/tasks.md` §Log pros 2 achados reais do teste (IA inventando token de memória — corrigido no prompt; e uma corrupção pré-existente no node do SDR, recuperada).
  - **Workflows ativos no n8n**: `MAG - Fase 0 (eco WhatsApp)` (id `ypeJKZLsGq1WxkQB` — identificação+buffer+SDR+handoff+**Operadora** com leitura e escrita, 40 nós), `MAG - Nutridora (T+0)` (id `3qI5VzAWMZbU2vly`), `MAG - Radar (resumo diário)` (id `kq6ULUF5lYU9HRQf`, spec 019 — só cron, sem AI Agent), `MAG - Nutridora (T+1/3/7)` (id `ZkAxwOPuWVxWncax`, spec 020 — idem) e `MAG - Avisar Equipe` (id `8PELlklNlOVVrTl9`, spec 012-T7 — sub-workflow chamado pela tool `avisar_equipe` do SDR via webhook próprio, `http://localhost:5678/webhook/avisar-equipe`). Filtro de números de teste (`991920338`, `5521979070319`, `5521964946079`) agora é **config por ambiente** (`$env.MAGMA_NUMEROS_TESTE_REGEX` — vazio em prod = sem filtro), não precisa mais editar nó nenhum pra abrir pra leads reais.
  - **Paridade dev/prod RESOLVIDA (2026-07-20, noite, antes do merge pra master)**: nós que chamavam Django hardcoded (`host.docker.internal`) agora usam hostname neutro **`magma-backend-interno`** (idêntico nos dois `docker-compose*.yml` via `extra_hosts`/alias de rede — decisão detalhada em `.context/decisoes.md`); os 3 segredos que ainda estavam hardcoded (X-Agente-Token, apikey Evolution × 2) viraram credencial n8n; os 2 workflows agora estão **versionados** em `plataforma/n8n/workflows/*.json` (checklist de import em prod: `plataforma/n8n/workflows/README.md`). Promover pra prod agora é só importar o JSON + recriar as 3 credenciais lá — zero edição de nó.
  - **3 bugs reais do n8n encontrados e documentados** (não repetir a investigação — ver Log de specs/010 e specs/012): (1) `AI Agent typeVersion 3.1` quebra com `toolHttpRequest` nessa build do n8n → usar `typeVersion 2.3`; (2) node com `typeVersion` trocado via API fica "não instalado" no editor visual (mas executa normal) → editar só via `n8n-mcp`; (3) `toolHttpRequest` com header manual (`sendHeaders`) quebra o schema pro Gemini → usar credencial `httpHeaderAuth`; (4) `toolHttpRequest` também quebra se o campo `url` usar expression `{{ }}` do n8n (parser de `{placeholder}` próprio do node interpreta mal) → usar valor literal, nunca `$env` nesse campo específico.
  - **⚠️ Incidente corrigido**: `manage.py test` sem `--settings=config.settings.test` lia o `N8N_LEAD_WEBHOOK` real de `backend/.env` e disparava WhatsApp de verdade pra números fictícios de fixture. Fix em duas camadas (settings de teste zera a var + Nutridora com filtro de número) — ver memória `testes-settings-isolado`. **Sempre `--settings=config.settings.test` ou `plataforma/rodar-testes.sh`.**
  - **Lacunas conhecidas pra próximas specs**: ~~agente não responde perguntas institucionais gerais~~ — **resolvido 2026-07-24, spec 017** (`info_institucional`); ~~toques agendados da Nutridora (T+1d/3d/7d)~~ — **resolvido 2026-07-24, spec 020** (`processar_nutridora` + `Schedule Trigger`); mais ações de escrita da Operadora (`atualizar_turma`/`atualizar_status_lead`, confirmação/PIN); ~~B5 Radar (resumo diário proativo)~~ — **resolvido 2026-07-24, spec 019** (`resumo_diario` + `Schedule Trigger`); `RegraAgente` (regras editáveis, hoje hardcoded no system prompt); opt-out dedicado da Nutridora (hoje só via `ContatoEscalado`/handoff).
  - **Próximo passo**: ~~promover pra prod as specs 013-020~~ — **feito em 24/07 à noite** (ver entrada "Deploy completo pra produção" abaixo). Resta: ~~cadastrar `ConfiguracaoAsaas` de PRODUÇÃO~~ (**feito pelo Daniel, informado em 25/07**); decidir com o Daniel mais ações de escrita da Operadora (`atualizar_turma`/`atualizar_status_lead`) pra continuar a Fase 1; spec 016-T9 (teste com mensagem fragmentada de verdade, agora que o buffer está em prod); investigar o output vazio do SDR em handoff (achado incidental, não corrigido).
- **Specs 024 e 027 (parcial) — IMPLEMENTADAS EM DEV (2026-07-25)**, direto do feedback do Daniel sobre a bateria de 6 conversas simuladas. Ver `historico/2026-07-25-specs-024-027-implementacao.md`.
  - **024 (tom e pesos) — T1..T13 entregues.** Só prompt + 2 nós, zero backend. Orçamento de regras mantido em 12 (as inegociáveis foram de 4 pra 5 ganhando o teto de formato; "SEU OBJETIVO" foi de 8 pra 7, com a regra da tool repetida virando `contextWindowLength` 10→20). Entrou: limiar de escassez em 3 vagas, ordem "convite antes de ficha", curso inexistente abrindo o leque, regra do nome positiva e sem sobrenome, proibição de prometer apuração, teto de ~600 caracteres. `Preparar contexto SDR` passou a aproveitar o `pushName`. **Validado em 19 turnos de conversa real em dev** — o antes/depois da Sandra e do Thiago está no histórico. Suíte: 289 testes OK.
  - **Um critério reprovou na 1ª rodada e virou ADR:** "apresente os cursos que existem, com o APH em destaque" foi obedecido como "fale só do APH". Só passou virando instrução contável com a leitura errada nomeada. É a 3ª vez que esse padrão aparece (ver `.context/decisoes.md`).
  - **027 (resiliência) — T2, T5, T6, T7 entregues, SÓ EM DEV.** `settings.executionTimeout: 60` no workflow (o nó de agente **não tem** opção de timeout, ao contrário do que o plano supunha), `EXECUTIONS_DATA_PRUNE` de 7 dias, imagem fixada em `2.31.5`, `mem_limit: 2g`. **T3 (watchdog) e T4 (alarme) NÃO foram feitos.** O pin de versão e o `mem_limit` de **prod ficaram em branco de propósito** — precisam de `n8n --version` e `free -m` rodados na VPS, senão fixar pode virar downgrade.
  - **Nenhum travamento nesta bateria** (0,19% de CPU, 513 MiB ao fim). A carga foi sequencial, então não é reprodução — mas reforça a hipótese de **concorrência** pra T1 da 027.
  - **A spec 025 ficou mais urgente do que a ordem 024→025→026 sugere:** em 3 dos 6 perfis o contato mandou mensagem depois de escalado e recebeu **silêncio** — inclusive perguntas triviais como "onde fica a escola?".
  - **Resíduo novo em dev** (nada apagado, decisão do Daniel): leads #32–#34 e `ContatoEscalado` dos números `55009000000{21..26,31}`, além dos antigos #9–#14 / leads #29–#31.
- **Spec 025 — HANDOFF DE PONTA A PONTA, IMPLEMENTADA EM DEV (2026-07-25), T1..T15**. Fecha os 3 buracos que a bateria expôs. (a) **`escalar_contato` virou o dono do lead**: garante o `Lead` por WhatsApp mesmo sem nome e sem curso (`garantir_lead()`, extraída do serializer pros dois caminhos usarem o mesmo dedup), com `status=em_atendimento`; antes, 3 de 6 contatos escalados **não existiam** em `leads_lead`. (b) **`ContatoEscalado` ganhou estado** (`resolvido_em`, `expira_em`, `unique` do número caiu) e `identificar_contato`/`processar_nutridora` passaram a olhar só os **ativos** — antes, ser escalado uma vez tirava o lead da Nutridora pra sempre; prazo em `ConfiguracaoSite.handoff_expira_horas` (24h, `0` = nunca). (c) **Cortesia 1×** via Redis `mag:cortesia:{numero}` — o silêncio que o Marcos e a Sandra levaram virou uma mensagem fixa, sem LLM e sem promessa de prazo. Mais: aviso ao gestor com **canal único** e o número anexado pelo workflow (não redigitado pela IA), e o critério **pagamento-neutro × objeção-de-preço** no `systemMessage`. Validado em conversa real: Rafael escala, Juliana e Bianca são respondidas com dado do FAQ. **Suíte 289 → 300 testes, verdes.** Admin ganhou "Resolver"/"Reabrir" em lote.
- **Harness de simulação de conversa VERSIONADO em `plataforma/n8n/simulacao/` (2026-07-25)** — viveu duas sessões no `/tmp` e foi **apagado no meio da implementação da 025**. Carrega conhecimento caro: payload `messages.upsert` da Evolution, `execution_data` serializado com `flatted`, caminho do `sqlite3` no pnpm store do container, e o truque dos números com **DDD 00** (não existem em WhatsApp real → o nó de envio falha de propósito → nenhuma bateria entrega mensagem a telefone de gente). Ver o README da pasta.
- **Spec 028 — LEAD QUENTE VAI ATÉ A MATRÍCULA, especificada em 2026-07-25 (PENDENTE de implementação)**. Nasceu de uma leitura do Daniel sobre a bateria: a Bianca abriu com *"já decidi que quero fazer"* e o sistema **parou de falar com ela** — a regra de handoff escala em "intenção clara de fechar matrícula em QUALQUER formulação". Caminho novo: mídia curada → `gerar_link_matricula` → pessoa preenche CPF e ganha a **carteirinha digital** → a MAG resume a conversa e pergunta ao gestor **no chat dele** se pode mandar o link de pagamento → gestor responde uma palavra → backend gera a cobrança e manda **pra lead**. Desenho da aprovação é do Daniel (*"o gestor responde diretamente ao bot que reorganiza/encaminha"*), e preserva a trava da spec 015 sem o atrito dela. **Princípio central: o modelo nunca escolhe o destinatário** — o número sai da `AprovacaoPendente`, não de parâmetro produzido pela IA. **Equilíbrio medido contra a bateria: 1 perfil dos 6 abre o caminho**, os outros 5 seguem no handoff. Não-conclusão fica com a Nutridora (zero código novo). **Ordem: 025 antes da 028** (a 028 usa o handoff consertado como caminho de saída); a 026 é opcional (sem ela o caminho funciona, só perde o reforço de desejo). 3 perguntas em aberto pro Daniel no `plan.md`.
- **Campanha digital 08/08** — objetivo nº 1. Studio + acervo prontos para produzir conteúdo; Social Maker em concepção (`docs/subsistemas/07-social-maker.md`, `07b-social-maker-manus.md`, `agente-social-maker-contexto.md`). **Tráfego pago (Meta Ads) planejado em 24/07** — plano só de aquisição, teto R$1.000, ver `docs/subsistemas/01b-trafego-pago-meta-ads.md`; rastreamento (pixel) da `specs/018-rastreamento-trafego-pago/` **implementado (T1-T4)**, falta T5 (teste manual do Daniel com Pixel ID real) e a campanha em si (Ads Manager, fora do alcance de um agente — precisa do login do Daniel).
- **Studio 2.0 — specs 002..005 IMPLEMENTADAS e VALIDADAS (2026-07-18, orquestração de agentes em 2 sessões)**: motor declarativo multi-formato (feed/story/capa_reel) + 6 templates (formação, depoimento c/ avaliações reais, vagas, formatura, educativo, capa de reel) + UI dinâmica c/ picker contextual + export kit + `marca.js`; app `apps.ia` (provedores cifrados, página "Integrações de IA", ✨ texto no Studio); Camada de Ações agent-first (`/api/acoes/` + `TokenAgente` — n8n já consegue pedir link de avaliação/status de turma por API). Validação: 62/62 render headless, 9/9 fluxos Django, 39/39 testes, browser real 6/6 templates + 3 bugs achados e corrigidos. Contratos consolidados em `docs/plataforma/03-api-contratos.md`. **Falta: specs 006 (pré-matrícula pública/imagem IA) e 007 (vídeo IA + render server-side)** — adapters de imagem/vídeo ainda não existem (capacidades respondem `false`, esperado).
- **Spec 008 — ACERVO EM CAMADAS, IMPLEMENTADA e VALIDADA (2026-07-18, noite)**: acervo virou da MARCA (`Midia` c/ camadas turma/curso/instrutores/estrutura/externa/geral; `credito` p/ imagem de internet), `Postagem` multi-contexto (turma/curso/marca), rotas gerais `/api/midia/acervo|acervo/camadas|acervo/enviar|postagens/` (rotas por turma intocadas — n8n/Manus ok), Mesa de Luz da marca + Studio da marca em `/dj-admin/midia/midia/acervo|studio/`, seletor de camada no picker do Studio (arte mistura fotos de camadas; templates de turma c/ `requer:['turma']` desabilitados no Studio da marca). Migração sem mover arquivo; suíte **50/50**; smoke real no browser (postagem da marca criada de ponta a ponta). **Próximos ligados a ela**: templates de conteúdo diário (divulgação de curso, instrutor, bastidores — "spec 009" do plano da sessão), calendário editorial (010), avaliações por curso no Depoimento.
- **Teste manual no browser** do Acervo (upload em massa real, dedup em lote, Studio→postagem→ZIP) — smoke test via test client passou; falta o exercício real.

## PENDÊNCIAS / QUESTÕES ABERTAS

1. ~~`design-system-junto/` é cópia idêntica de `design-system/`~~ — **resolvido 2026-07-19**: pasta removida (do working tree e do histórico do git); `design-system/` na raiz é a única fonte.
2. Seed inicial completo (`docs/plataforma/08-conteudo-inicial-seeds.md`) ainda não materializado como comando idempotente.
3. Prod: aplicar `client_max_body_size 1g` no nginx do HOST (ref. já está em `plataforma/nginx/`).
4. Itens de mídia enviados antes do dedup não têm `meta.nome_original` (não entram na checagem; sem backfill, não crítico).
5. Painel do gestor fora do Django Admin (docs 06) — futuro; por ora o Admin é o painel.
6. Porta 8000 local costuma estar ocupada por outra app — `init-dev.sh` já lida/atenção ao subir.
7. ~~Correção de registro: v0.1 sem testes persistidos~~ — **resolvido 2026-07-19**: spec 001 implementada, 120 testes cobrindo todos os apps (ver linha "Rede de segurança" na tabela PRONTO acima).
8. **Teste manual do Daniel no Studio 2.0**: gerar artes reais dos 6 templates (feed+story), configurar um provedor de IA na página nova e testar o ✨. A validação automatizada passou; falta o olho do dono.

## METAS (repensadas na baseline v0.1)

1. **Curto (até 08/08/2026):** campanha digital → turma de Socorrista APH cheia. Tudo que acelera isso tem prioridade (conteúdo via Studio, Social Maker, prova social das avaliações, LP afiada).
2. **Médio:** gestão escolar operável pelo celular (matrícula, presença, pagamentos) + certificação/carteirinha na plataforma (aposentar protótipos de vez).
3. **Longo:** "colégio digital da saúde da Baixada" — híbrido, comunidade/assinatura, agentes de IA operando atendimento/marketing/conteúdo (ver `plano-evolucao-digital-magma.md`).
