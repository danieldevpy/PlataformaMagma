# Log de decisões (ADR curto)

> Formato: data · decisão · porquê. Não rediscutir sem fato novo.
> Decisões táticas de uma frente específica podem viver no `docs/subsistemas/NN-execucao-status.md` da frente; aqui ficam as que afetam o projeto inteiro.

- **2026-07-15** · Monorepo `plataforma/` com Django/DRF + Next.js App Router, conteúdo seed-first (LP roda idêntica ao template com tudo vindo do banco). *Porquê:* gestor edita conteúdo pelo painel sem dev; ver constituição §1–2.
- **2026-07-16** · Prod reformulado: nginx no HOST da VPS, containers em portas loopback, split `api.ts` SSR/browser, URLs de mídia relativas. *Porquê:* simplifica TLS/roteamento e corrige mídia quebrada em dev.
- **2026-07-16** · Links de avaliação/carteirinha em dois escopos: **turma (compartilhado, default)** e individual. *Porquê:* distribuir 1 link no grupo da turma é o fluxo real do instrutor.
- **2026-07-17** · Páginas staff (Acervo/Studio) no namespace `/dj-admin/` via `TurmaAdmin.get_urls`. *Porquê:* nginx de prod só roteia `api|dj-admin`; zero rota nova no host.
- **2026-07-17** · Upload de mídia: 1 arquivo/request, máx 1 GB, vídeo sem transcode; duplicado detectado por nome+tamanho → 409 com `forcar=1` para sobrepor. *Porquê:* simplicidade e proteção contra retrabalho de curadoria.
- **2026-07-17** · Regra-guarda de CSS: todo stylesheet novo que esconde elemento via atributo `hidden` inclui `[hidden]{display:none !important}`. *Porquê:* regras de autor `display:flex` vencem o UA stylesheet — causou modal fantasma no Acervo.
- **2026-07-18** · Reorganização: protótipos (`landing-page/`, `avaliacao/`, `carteirinha-digital/`, `studio/`) movidos para `mvp-apps/` como referência histórica; produto vive só em `plataforma/`. *Porquê:* separar laboratório de produto.
- **2026-07-18** · **n8n adotado como orquestrador de automações** (agente WhatsApp, follow-up de leads). Banco: **SQLite interno do n8n** em dev e prod — o n8n não suporta MySQL (só SQLite/Postgres), então NÃO reaproveita o banco da plataforma; Postgres dedicado só se houver queue mode/alto volume. Prod: serviço no `docker-compose.prod.yml` (loopback 5678) + subdomínio `n8n.<dominio>` no nginx do host; dev: compose avulso via `init-dev.sh --n8n`. `N8N_ENCRYPTION_KEY` é fixa e nunca deve ser trocada. *Porquê:* setup mínimo correto, sem Redis/workers. Detalhe: `plataforma/n8n/README.md`.
- **2026-07-18** · Portas de prod corrigidas para loopback real (`127.0.0.1:8000/3000`) — o compose publicava aberto, divergindo do comentário/arquitetura documentada. *Porquê:* só o nginx do host deve alcançar os containers.
- **2026-07-18** · **Baseline v0.1.0** (tag git) + estrutura de memória do projeto: `CLAUDE.md` raiz, `.context/` (dotcontext), `.specify/memory/constitution.md`, `specs/` com templates, histórico de prompts em `.context/historico/`. *Porquê:* desenvolvimento por agentes com memória persistente — saber sempre de onde veio e onde parou.
- **2026-07-18** · **Studio 2.0 — motor de templates declarativo sem DOM**: `templates/*.js` só recebem `ctx` Canvas 2D + `dados` JSON-serializáveis (fotos por `imgKey` + `assets.imagens` Map); DOM/carregamento só no adaptador do core. `renderizar()` valida formato pedido contra `formatos` declarados. *Porquê:* mesmo template renderiza no browser hoje e em Node/server na spec 007, sem reescrever.
- **2026-07-18** · **Credenciais de provedores de IA cifradas com Fernet derivado de `DJANGO_SECRET_KEY`** (write-only: nunca voltam na API; em branco no PATCH mantém a salva). *Porquê:* chave nunca no browser e zero segredo novo pra gerenciar. **Consequência operacional: rotacionar `DJANGO_SECRET_KEY` em prod invalida as credenciais salvas — recadastrar depois.**
- **2026-07-18** · **Camada de Ações agent-first no `nucleo`**: registry único (`/api/acoes/`), auth por `TokenAgente` (hash sha256, mostrado 1x, escopos `app:acao`/`app:*`/`*`), `LogAcao` sempre (inclusive erro), 403 antes de 404 p/ não vazar existência. Retornos usam IDs públicos (código de turma), nunca PK. *Porquê:* humano e agente (n8n/WhatsApp) usam a mesma API auditável.
- **2026-07-18 (noite)** · **Acervo em camadas (spec 008)**: `MidiaTurma` → `Midia` com `camada` (`turma|curso|instrutores|estrutura|externa|geral`), turma/curso opcionais por invariante, `Postagem` multi-contexto (turma/curso/marca); rotas por turma preservadas, rotas gerais novas; arquivos legados NÃO mudam de lugar (upload_to ramifica). *Porquê:* produção de conteúdo diária mistura fotos de turmas, curso, instrutores e banco de imagens — o modelo turma-cêntrico do 09 não comportava. Decisões dentro dela: **turma segue CASCADE** (apagar turma apaga a mídia dela — consentimento/LGPD acima de conveniência); **consentimento continua sendo da turma** (foto com aluno identificável mora na camada turma); imagem de internet exige `credito`; `FotoCurso` fica como está por ora e converge pro acervo no futuro (evitar 3º silo — reavaliar quando a LP for consumir acervo).
- **2026-07-20** · **Agente WhatsApp (plano `docs/subsistemas/02b-agente-whatsapp-n8n.md`) — decisões do §10 batidas pelo Daniel**: (1) gateway Evolution API confirmado (já implementado, Fase 0); (2) número dedicado de teste fica só de teste — número oficial de produção é decisão futura, separada; (3) persona pública única **"MAG"**, squad de subagentes invisível por trás; (4) LLM dos agentes **configurável/trocável** (não fixar um provedor no código) — modelo escolhido via credencial/parâmetro no node do n8n (LangChain/AI Agent), não hardcoded como o `apps.ia` faz hoje pro Studio, pra Daniel poder comparar custo-benefício sem precisar de deploy; (5) agenda de visitas do SDR (A1) via **Data Table do n8n** (MVP, sem depender de Google Calendar); (7) Postgres dedicado da Evolution aceito (já implementado). *Porquê:* fecha o bloqueio da Fase 1 (squad de atendimento) — decisão #6 (auth do upload de acervo por agente) ficou como recomendação técnica (reaproveitar `X-Agente-Token` na rota REST existente), não bloqueia por ser barata de mudar depois.
- **2026-07-21 (noite)** · **Promoção de workflow n8n dev→prod virou script (`plataforma/n8n/workflows/promover-prod.sh`)**, depois de fazer manualmente a 1ª vez na entrega da spec 013: achar o ID do workflow existente em prod, mapear os IDs das 3 credenciais (X-Agente-Token, Evolution apikey, Gemini — cada instância n8n tem os seus próprios IDs mesmo com nome idêntico), remontar o JSON, importar via CLI (`n8n import:workflow`), publicar (`n8n publish:workflow` — este n8n usa modelo draft/publish, não só um bit `active`) e reiniciar o container (publicação só pega efeito após restart em modo não-cluster). Os IDs descobertos ficam versionados em `plataforma/n8n/workflows/ids-prod.json` (não são segredo — só identificador de linha do banco) pra nunca mais precisar redescobrir. *Porquê:* virou um comando único (`promover-prod.sh <arquivo.json>`) em vez de ~20 passos manuais + investigação; reduz risco de erro humano numa operação que mexe em produção. **Efeito colateral aceito:** o restart do n8n derruba o processamento de webhook por alguns segundos — aceitável pro volume atual, revisar se o agente crescer (ex.: n8n em modo multi-main evitaria o restart).
- **2026-07-20 (noite)** · **Paridade dev/prod dos workflows n8n do agente MAG, resolvida ANTES do merge pra master** (pedido explícito do Daniel, pra não ter retrabalho subindo pra produção): (a) URL do Django nos nós vira hostname neutro **`magma-backend-interno`**, idêntico nos dois ambientes (dev: `extra_hosts` do n8n dev → `host-gateway`; prod: alias de rede no serviço `backend`) — tentativa inicial foi usar `$env.MAGMA_BACKEND_URL` numa expression `{{ }}`, mas o node `toolHttpRequest` (tools da IA) tem parser de `{placeholder}` PRÓPRIO que interpreta mal chaves duplas `{{ }}` dentro do campo `url`, quebrando a validação (`UNDEFINED_PLACEHOLDER`) — hostname literal idêntico nos dois ambientes evita esse conflito por completo; (b) filtro de números de teste (regex nos 2 nós IF) migrou pra `$env.MAGMA_NUMEROS_TESTE_REGEX` (esse SIM funciona via env var, por não estar num campo `toolHttpRequest`) — vazio em prod = regex vazia = sem filtro; (c) os últimos 3 segredos hardcoded (`X-Agente-Token` em "Identificar Contato", apikey da Evolution em 3 nós de envio) migraram pra credencial n8n, igual já era feito em `escalar_contato`/`avisar_equipe`; (d) os 2 workflows exportados como JSON versionado em `plataforma/n8n/workflows/` (não existia versionamento nenhum antes — só viviam no banco do n8n). *Porquê:* até então promover pra prod exigiria caçar e editar ~8 valores hardcoded workflow por workflow, sem histórico de mudança nenhum. **Incidente durante o próprio trabalho**: as duas primeiras rodadas de `n8n_update_partial_workflow` (sanitização automática da ferramenta) zeraram sem querer os `parameters` do node "SDR - Capitã de Matrículas" (systemMessage inteiro perdido) — detectado só ao rodar um teste real pós-mudança (resposta "No prompt specified"), recuperado batendo o version history do próprio n8n (`n8n_workflow_versions`) até achar a versão anterior à edição. Lição: **depois de qualquer `n8n_update_partial_workflow` em lote, comparar `parameters` de TODOS os nós contra o estado anterior antes de considerar a mudança segura** — a sanitização automática da ferramenta pode ter efeito colateral em nós não referenciados na operação.
- **2026-07-23 (madrugada seguinte)** · **Buffer de mensagens fragmentadas do WhatsApp (debounce) usa Redis novo e dedicado** (`magma-n8n-redis-dev`/`n8n-redis`), não o Redis já existente da Evolution API. *Porquê:* o projeto já tem o princípio de "infra dedicada, nada reaproveitado entre serviços" (mesmo padrão do Postgres/Redis da Evolution) — misturar o cache interno da Evolution com o buffer do n8n criaria acoplamento desnecessário entre dois serviços que evoluem de forma independente. Padrão de implementação: cada mensagem entra numa lista Redis (`wpp:buffer:{numero}`), a execução marca `wpp:lastseen:{numero} = $execution.id` e espera N segundos; ao acordar, só processa se ainda for a "última vista" (senão aborta — a execução mais nova assume). Ainda protótipo (ver `historico/2026-07-23-buffer-mensagens-whatsapp-prototipo.md`), decisão de virar spec formal e promover pra prod fica em aberto.
- **2026-07-23** · **Spec 015 (Asaas) — app novo `apps/financeiro/`, não dentro de `educacional`**; reusa `apps.ia.crypto.cifrar/decifrar` pra credencial e token de webhook cifrados (é genérico, mover pra `nucleo` seria só churn nesta spec); sem model `ClienteAsaas` (cliente resolvido por `externalReference` em runtime, 1 chamada HTTP extra em vez de mais uma tabela/ambiguidade sandbox×produção). **`POST /api/financeiro/webhook/asaas/` é o 1º webhook HTTP externo do projeto** — sem precedente prévio de auth por assinatura/token vindo de fora; o padrão escolhido (header customizado comparado contra token cifrado no banco, sempre 200 pra requisição autenticada mesmo se a cobrança não for achada, todo evento auditado em `EventoWebhookAsaas`) fica registrado aqui pra próximas integrações (ex.: outro gateway) seguirem sem reabrir a discussão. *Porquê:* pagamento é dado sensível — nunca gerar cobrança real por engano (só 1 `ConfiguracaoAsaas.ativo` por vez, entre sandbox e produção) e nunca perder rastro de por que um status mudou. **Testado de ponta a ponta com credencial sandbox real** (túnel ngrok, já que dev não tem URL pública): webhook chegou de verdade e atualizou `Cobranca` pra `paga` sem nenhuma intervenção manual.
- **2026-07-24** · **1ª campanha de tráfego pago (Meta Ads) — só aquisição, teto R$1.000, objetivo Mensagens direto (sem CAPI nesta fase)**. Batido com o Daniel depois de analisar um guia gerado pela Manus: nada de campanha de retenção/reativação agora (sem base de retargeting madura, sem tempo até 08/08); orçamento não passa de R$1.000 no ciclo inteiro, reinvestindo conforme a campanha gerar matrícula; objetivo de campanha é **Mensagens/Click-to-WhatsApp** (não Leads/Tráfego pro site) — não depende de Pixel pra otimizar e cai direto no canal que já é maduro (MAG, specs 010/013/014). Rastreamento fica limitado a **Pixel client-side** (`PageView`+`Contact`+`Lead`, spec 018) — sem Conversions API, sem Custom Audiences/Lookalike nesta fase. *Porquê:* 15 dias de janela até 08/08 não comportam a estrutura de funil completo (prospecção+retargeting+retenção) nem o cronograma de escalonamento de 5+ semanas que o guia original propunha; CAPI e retargeting exigem dado que ainda não existe (pixel nunca esteve instalado) e são mais valiosos depois que houver caixa pra reinvestir. Ver `docs/subsistemas/01b-trafego-pago-meta-ads.md` (plano completo) e `specs/018-rastreamento-trafego-pago/` (implementação do pixel, PENDENTE).
- **2026-07-24 (noite)** · **Conversa do agente vira dado da plataforma, com retenção configurável no Admin (spec 021)**. App novo `apps/conversas/` (`Conversa` + `Turno`) porque a conversa não é só de lead — gestor fala com a Operadora e aluno matriculado também fala com a MAG; pendurar em `leads` forçaria um vínculo que nem sempre existe. Três decisões dentro dela: (a) **o registro acontece DEPOIS do envio no WhatsApp, num nó só por pista**, aproveitando que os dois AI Agents já tinham `returnIntermediateSteps: true` — a saída do agente carrega texto final **e** ferramentas chamadas, então um `HTTP Request` com `onError: continueRegularOutput` grava os dois turnos sem custar latência pro lead nem virar ponto de falha no caminho crítico (se o Django cair, a MAG continua vendendo e só perde o registro); (b) **`desfecho` derivado por código das ferramentas chamadas** (`registrar_lead`→lead, `escalar_contato`→handoff, `matricular_aluno`→matrícula, `gerar_cobranca`→cobrança, sempre o ponto mais avançado do funil) — a informação já está nas tools, classificar com LLM seria caro, lento e menos exato (mesma lógica do Radar da spec 019 não ter AI Agent); (c) **retenção em `ConfiguracaoSite.conversas_retencao_dias`** (padrão 15, `0` = nunca), não em variável de ambiente — pedido explícito do Daniel de "só mudar uma config": env var em prod exigiria editar `.env.prod` na VPS e recriar container, campo no Admin é três toques no celular. Purga pendurada no fim do Radar diário (rotina que já existe e já está ativa nos dois ambientes), depois do envio do resumo pra falha na purga nunca comer o relatório. *Porquê:* sem isso, avaliar se a MAG responde certo e converte exigia abrir execução por execução no n8n — não dá pra analisar em bloco nem entregar pra uma LLM ler. Motivador imediato: a campanha de tráfego pago (spec 018) traz os leads mais valiosos pra calibrar o agente. **LGPD:** conversa de venda com pessoa real, então retenção com prazo + purga automática + acesso só a staff (Admin somente leitura) + dado na VPS da própria Magma.
- **2026-07-24 (noite)** · **Memória de conversa dos agentes migra de `memoryBufferWindow` (RAM do n8n) pra `memoryRedisChat` (spec 022)**, reusando o Redis e a credencial que o buffer da spec 016 já tinha nos dois ambientes. Ideia do Daniel ao revisar a spec 021 ("não daria pra aproveitar o Redis, já que subimos um container pra ele?") — e derrubou a alternativa Postgres Chat Memory que estava cogitada, porque a credencial `redis` **já existia em prod** (`ids-prod.json`), eliminando o único passo manual da promoção (criar credencial no editor de produção nunca deu pra automatizar com segurança). `sessionTTL: 21600` (6h) = expiração nativa do Redis, sem cron nem código, e **igual à janela de sessão da spec 021** de propósito: os dois lados precisam concordar sobre o que é "uma conversa". *Porquê:* a Simple Memory vivia na RAM do processo — todo restart do n8n zerava o contexto de todos os leads em conversa, e restart não é raro (o `promover-prod.sh` reinicia o container a cada promoção). Lead quente sendo tratado do zero no meio da venda é perda silenciosa. **Achado durante a implementação:** com `sessionIdType: customKey` a chave no Redis é literalmente o valor da expression, então SDR e Operadora — que usavam o mesmo `{{ $json.numero }}` — passariam a **compartilhar memória** (coisa que não acontecia com a Simple Memory, onde cada nó tinha seu próprio store em RAM). Corrigido prefixando por agente: `mag:sdr:{numero}` e `mag:operadora:{numero}`. **Risco aceito e registrado:** o Redis agora segura duas funções (buffer + memória) — não é ponto único de falha novo (o buffer já está no caminho crítico de toda mensagem desde a spec 016), mas o estrago de uma queda fica maior.
- **2026-07-24 (madrugada)** · **`curso_slug` vindo do agente de IA passa a ser resolvido por semelhança, não por match exato (spec 023)**. Achado real: a MAG **nunca acertou o slug** — mandou `socorrista-aph-120h`, `aph-120h` e `socorrista-aph-120h` em três execuções, sempre montando o identificador a partir do nome exibido ("Socorrista APH (120h)"), mesmo tendo `"slug": "socorrista-aph"` no contexto vindo de `detalhes_curso`. Reforçar o system prompt não resolveu, e dar descrição ao campo da tool (que não tinha nenhuma) também não. `resolver_curso()` (`apps/leads/serializers.py`) agora tenta match exato e, falhando, pontua por sobreposição de termos contra slug+nome de cada curso, exigindo **vencedor único** — no empate devolve `None`, porque lead sem curso é ruim mas lead com o curso ERRADO é pior. Nunca levanta erro (perder o lead é pior que perder o curso) e sempre loga `warning`. *Porquê:* identificador técnico exato é justamente o tipo de dado que LLM erra — ele acerta o ASSUNTO e erra a string. O backend engolia o erro em silêncio (`filter(slug=...).first()` → `None`), então slug inventado era indistinguível de slug ausente, e o lead nascia sem curso, quebrando o toque T+1 da Nutridora (spec 020), que monta a mensagem com as habilidades reais do curso. **Princípio generalizável pras próximas tools:** toda vez que uma tool receber identificador técnico gerado pelo modelo, o backend resolve com tolerância e **registra quando teve que corrigir** — nunca falha calado.
- **2026-07-24 (madrugada)** · **Integridade de dado do agente é garantida no backend, não no system prompt (spec 023)**. O prompt do SDR **já dizia** "registre o lead uma vez só por conversa" e mesmo assim `registrar_lead` foi chamado 2× na mesma conversa, criando 2 `Lead` pra mesma pessoa. `LeadPublicoSerializer.create` passou a buscar-ou-atualizar por `whatsapp` (nunca deduplica com whatsapp vazio — colapsaria todos os leads sem número num só; preserva `criado_em` e `nutridora_ultimo_toque` pra não reiniciar a régua da Nutridora nem inflar "leads das últimas 24h" do Radar; valor vazio não sobrescreve dado bom). Efeito colateral desejado: quem preenche o formulário da LP duas vezes também para de virar dois leads. *Porquê:* prompt é orientação probabilística — serve pra economizar chamada e guiar o tom, nunca pra garantir invariante de banco. `Lead` passa a significar "pessoa interessada"; o histórico de cada interação vive em `apps/conversas` (spec 021).
- **2026-07-25** · **Escassez só é argumento quando é escassez de verdade — limiar de 3 vagas (spec 024)**. Decisão do Daniel lendo a bateria de 6 conversas simuladas: a MAG abria a conversa com *"temos apenas 14 vagas disponíveis"* numa turma de 15 lugares — *"isso representa 70% da turma de forma irônica, torna irrelevante para o usuário, eu mesmo iria achar até chato"*. A regra 3 do system prompt mandava "criar senso de urgência legítimo com dado real", e 14 **é** dado real — só não é urgência. Passa a valer: `vagas_restantes ≤ 3` cita o número; acima disso, **não menciona vagas em nenhuma forma** (nem "ainda temos vagas"). *Porquê:* escassez falsa gasta a credibilidade da escassez verdadeira — quando a turma encher de fato, a frase precisa significar alguma coisa. Cogitado e descartado calcular no backend (campo `escassez_relevante` no payload): `GET /api/cursos/{slug}/` é o mesmo contrato que a LP consome e não é lugar de conselho de venda; além disso comparar inteiro com 3 não é a classe de erro que a spec 023 diagnosticou (lá era **montar string de identificador**, coisa que LLM erra sistematicamente).
- **2026-07-25** · **O orçamento de regras do system prompt é fixo: entra uma, sai uma (spec 024)**. Princípio dado pelo Daniel junto com a aprovação das correções: *"quanto mais regra colocamos, mais engessados deixamos... apenas mexa alguns pesos, porque na grande maioria do tempo ele foi cirúrgico"*. Toda correção de comportamento da MAG passa a ser feita **fundindo ou reescrevendo** regra existente, não empilhando restrição nova. Corolário aprendido em duas rodadas (spec 023 e a bateria de 25/07): **regra proibitiva por lista de exemplos vaza** — proibido "registrei/cadastrei/salvei" virou *"pra deixar seu cadastro certinho aqui"*. Regra que veda comportamento tem que ser **positiva** (o que fazer), não uma lista do que não dizer: *"pergunte o nome e mais nada"* fecha a porta que *"não diga X, Y, Z"* deixa aberta. *Porquê:* prompt é orçamento de atenção do modelo, não documento de requisitos — cada regra nova dilui as anteriores, e as que a spec 023 provou funcionar (handoff amarrado à ação, `curso_slug` no registro) valem mais que qualquer regra nova.
- **2026-07-25** · **Objeção de preço escala pro humano; pergunta neutra sobre pagamento a MAG responde (spec 025)**. Confirmado pelo Daniel corrigindo uma conclusão errada da minha análise (eu tinha classificado como falso positivo a escalada do lead que disse *"650 tá salgado pra mim mano"*): *"é muito importante que o gestor tome a frente quando o cliente não aceita o preço sugerido pela empresa"*. Critério: *"como pago? / dá pra parcelar? / aceita PIX?"* → responde pelo FAQ do curso; *"tá caro / tem desconto? / consegue por menos?"* → escala. *Porquê:* preço é negociação, e quem negocia preço é a escola — não é dúvida operacional que uma FAQ resolve. Efeito na leitura da bateria: das 6 escaladas observadas, 4 estavam **certas**; o problema real não é a MAG escalar demais, é o handoff não ter rede embaixo dele (daí a spec 025).
- **2026-07-25** · **`ContatoEscalado` deixa de ser "existe = silenciado" e passa a ter estado (spec 025)**. O docstring original era explícito ("a presença do registro já é o estado; apagar pelo admin = libera") e era a simplificação certa pra spec 012, quando handoff era exceção. Na bateria de 25/07, **6 de 6 conversas escalaram** — o handoff virou o fluxo principal, e "apagar pra liberar" passou a ser o único caminho de volta (perdendo o histórico de que houve handoff). Passa a ter `resolvido_em` + `expira_em`, com prazo configurável em `ConfiguracaoSite` (mesmo padrão da retenção de conversas da spec 021, "só mudar uma config"); `unique` no número cai (senão o segundo handoff do mesmo contato colide com o registro resolvido). Consequência obrigatória: `processar_nutridora` (spec 020) passa a excluir só os escalados **ativos**. *Porquê:* o pior caso observado foi um contato reclamar de não ter resposta, ser escalado, perguntar o preço e receber **silêncio permanente** — o sistema respondeu à reclamação com exatamente o problema reclamado, agora definitivo.
- **2026-07-25** · **No handoff, o lead é garantido pelo backend — `escalar_contato` cria-ou-atualiza o `Lead`, mesmo sem nome e sem curso (spec 025)**. Fecha a questão que a spec 023 deixou aberta pra decisão do Daniel, com a evidência do pior caso: na bateria de 25/07, o contato que disse *"quero garantir minha vaga"* (nome dado, curso identificado) **não virou lead** — 3 dos 6 ficaram só em `ContatoEscalado` + `Conversa`, fora de `leads_lead`, e portanto fora do Radar e da Nutridora. A regra de handoff manda parar de qualificar e a MAG parava antes de registrar. Contrapartida aceita conscientemente: **leads magros na base** (só WhatsApp, sem nome) — lead sem nome ainda é um número pra ligar e já está silenciado pro automático, enquanto perder o contato mais quente não tem conserto. *Porquê:* mesma lição da spec 023 — invariante de dado não pode depender de instrução no prompt; se depender, uma hora falha.
- **2026-07-25** · **Aviso de handoff instrui o gestor a responder pelo WhatsApp da empresa, nunca pelo pessoal (spec 025)**. Pedido do Daniel: *"a pessoa não tem que ter 2 vias de comunicação"*. Hoje `avisar_equipe` cai no WhatsApp pessoal de cada gestor (spec 012 + adendo 019-T9) e o caminho natural é responder dali. Além da experiência ruim pro contato, duas vias quebram o registro da spec 021 (metade da conversa não é gravada), cegam a Nutridora e deixam o relacionamento no aparelho de quem atendeu. Fica registrado como **limitação conhecida**: isso é orientação no texto do aviso, não trava — trava real exigiria caixa de entrada compartilhada, outro produto.
- **2026-07-25** · **A MAG pode mostrar mídia, mas só a que o gestor curou a dedo (spec 026)**. Ideia do Daniel na revisão da bateria: em vez de despejar ficha técnica pra cativar, oferecer *"foto/vídeo escolhido a dedo pelos gestores"*. A curadoria entra como tag nova em `Midia.tags` (mesmo mecanismo de `destaque`/`capa`/`avaliacao` da Mesa de Luz, spec 008) — zero migração, zero tela nova, mesmo gesto que o gestor já faz. **A IA decide a hora de oferecer, nunca o que existe pra oferecer.** Camada `turma` fica **fora por padrão** (pessoas identificáveis; o consentimento que existe é pra álbum e divulgação curada, não pra disparo em atendimento individual) e camada `externa` também (crédito obrigatório não cabe em mensagem de WhatsApp). Desenho forçado por limitação conhecida do n8n: como `toolHttpRequest` não aceita expression `{{ }}` no campo `url` (achado de 2026-07-20) e a URL da mídia **não** pode ser idêntica em dev e prod, a tool só **consulta** e quem envia é um nó `httpRequest` comum do fluxo — mesmo padrão que a spec 012 usou pro `avisar_equipe`, pelo mesmo motivo raiz.
- **2026-07-25** · **Travamento do n8n em dev: causa raiz indeterminada, blindagem entra assim mesmo (spec 027)**. Durante a bateria de conversas o n8n parou por inteiro (1 núcleo a 100%, event loop bloqueado, webhook fora do ar, memória plana em 580 MiB) e só voltou com `restart`. Descartados **por medição**, não por suposição: falta de memória (limite de heap 4.288 MiB, container sem limite — a mensagem `"possible out-of-memory issue"` gravada na execução é o texto genérico de recuperação do n8n, não diagnóstico), backend fora (Django respondeu 200 em 18ms durante o travamento), crash do processo (ficou vivo girando) e GC em espiral (consumiria memória até o teto). Sobra assinatura de **laço síncrono na thread principal**, no nó do agente, logo depois de uma tool retornar em 28ms. *Decisão:* investigação vira T1 de spec própria, mas as blindagens que valem independentemente da causa (timeout no nó do agente, watchdog que reinicia de verdade — `healthcheck` do compose **não** reinicia nada e `restart: unless-stopped` não cobre processo vivo e travado —, alarme no WhatsApp, poda de execuções e imagem com versão fixada em vez de `latest`) entram **antes** de a campanha de tráfego pago escalar. *Porquê:* prod roda a mesma imagem e os mesmos workflows com **mais** concorrência (cada mensagem fragmentada vira uma execução por fragmento — 10 pessoas ao mesmo tempo = 30 execuções; no travamento havia 3), sem nenhum alarme, e mensagem que chega durante o travamento **some** (o WhatsApp não retenta). Nota metodológica: a poda de execuções só ficou segura depois da spec 021, que moveu o histórico que importa pro Django — as execuções do n8n voltaram a ser o que sempre deveriam ter sido, depuração de curto prazo.
- **2026-07-25 (implementação)** · **Timeout de resiliência do n8n é da EXECUÇÃO, não do nó do agente (spec 027-T2)**. O plano da 027 partia de que "os nós `agent` do n8n aceitam timeout" — **não aceitam**: nem `@n8n/n8n-nodes-langchain.agent` nem o nó do modelo (`lmChatGoogleGemini`) expõem propriedade de timeout em versão nenhuma. O que existe é `settings.executionTimeout` do workflow, posto em **60s** no `mag-fase-0-sdr.json`. *Porquê:* cobre mais do que se pedia (qualquer nó pendurado, não só o agente), viaja pra prod junto com o workflow (o `_limpar_export.py` preserva `settings`, então o `promover-prod.sh` leva junto sem passo manual) e não depende de env var. Margem folgada: execução medida leva ~12s no pior caso (5s de debounce + 2,5–6,5s de agente), então 60s só dispara em anomalia real. *Consequência:* a execução travada passa a **falhar** em vez de ficar pendurada — e falha é observável, que era o ponto.
- **2026-07-25 (implementação)** · **Mudança de infra em produção não se escreve às cegas a partir do que dev mostra.** As blindagens de poda de execução (aditivas, sem risco) foram escritas nos dois composes, mas o **pin de versão e o `mem_limit` ficaram só em dev**, com um bloco de comentário no `docker-compose.prod.yml` dizendo exatamente o que rodar na VPS antes de preencher (`n8n --version` e `free -m`). *Porquê:* dev roda 2.31.5; se prod já estiver mais novo, fixar em 2.31.5 é **downgrade**, e migração de banco do n8n não volta atrás — um arquivo com valor plausível mas não verificado é pior que um campo em branco, porque o próximo deploy o executa sem ninguém reler. Mesma lógica pro `mem_limit`, que depende da RAM da VPS.
- **2026-07-25 (implementação)** · **Instrução vaga sobre ênfase o modelo resolve a favor da ênfase (spec 024-T4)**. A regra "apresente os cursos que existem, com o APH em destaque como carro-chefe" foi obedecida como "fale só do APH" — exatamente o vício que ela existia pra corrigir, e reproduzido em teste real. Só passou quando virou instrução contável com o erro nomeado: "LISTE PELO NOME TODOS os cursos que vieram de `listar_cursos` — todos mesmo, não só o APH (...) responder 'nosso foco é o APH' e parar por aí é exatamente o erro". *Porquê:* é a terceira vez que o mesmo padrão aparece (spec 023: slug inventado; 024: lista de frases proibidas contornada por sinônimo; agora ênfase virando exclusividade). **Princípio pras próximas edições de prompt:** quando a regra tem um "mas" ou uma hierarquia dentro dela, o modelo colapsa pro lado enfatizado — a regra precisa dizer o que **contar/listar** e nomear explicitamente a leitura errada que se quer barrar.
- **2026-07-25** · **Lead quente não é escalado — é conduzido até a matrícula, e o gestor aprova o pagamento pelo chat dele com o bot (spec 028)**. Leitura do Daniel sobre a bateria: a Bianca abriu com *"já decidi que quero fazer"* e o sistema **parou de falar com ela** (a regra de handoff manda escalar em "intenção clara de fechar matrícula em QUALQUER formulação"), no melhor momento que ela ia ter. Passa a existir um caminho novo — mídia curada → link de matrícula (`/carteirinha/nova/{token_cadastro}`, spec 014) → carteirinha digital → pedido de aprovação ao gestor → cobrança. **O desenho da aprovação é do Daniel** e substituiu as três opções que eu ofereci (parar na matrícula / ir sozinha até o pagamento / só mídia): *"o gestor responde diretamente ao bot que reorganiza/encaminha (...) aí evita que o humano tenha que acessar o wpp do curso"* — a MAG resume a conversa, manda pro gestor **no chat dele** (que já cai na pista da Operadora), e ele decide com uma palavra. *Porquê:* preserva integralmente a trava da spec 015 (cobrança nunca sai sem humano confirmar valor e destinatário) e elimina o atrito que ela custava — o gestor deixa de precisar trocar de conversa, ler o histórico e responder pelo WhatsApp da escola. **Decisão de desenho que sustenta o resto: o modelo NUNCA escolhe o destinatário.** O gestor responde "pode mandar" e nada nessa frase diz pra quem; se houvesse uma tool `enviar_mensagem(numero, texto)`, o modelo teria que **produzir** o número da lead — exatamente a classe de erro que a spec 023 documentou (identificador técnico montado de cabeça). Então o número entra na `AprovacaoPendente` vindo do contexto da execução, e quem envia é o **backend** chamando um webhook do n8n (mesmo padrão da Nutridora T+0, spec 011). O único identificador que a IA produz é o id da aprovação, e ele vem de uma lista que o backend acabou de devolver. **Equilíbrio, a pedido explícito do Daniel** (*"para outros perfis isso tem que ser equilibrado"*): o caminho só abre com **duas condições juntas** — intenção *declarada* (não "quanto custa?", mas "quero fazer") **e** nenhuma objeção aberta na conversa; qualquer objeção posterior fecha o caminho e vira handoff. Medido contra a bateria, isso é **1 perfil dos 6** — de propósito, é o único em que o humano não acrescenta nada e só atrasa. Objeção de preço continua indo pro gestor, como o Daniel já tinha confirmado. **Não-conclusão da matrícula fica com a Nutridora** (régua T+1/3/7 da spec 020), sem tratamento especial e sem código novo — a pessoa virou `Lead` e não está escalada, então a régua a pega naturalmente.
- **2026-07-25 (spec 025 implementada)** · **O handoff ganhou estado, e o `escalar_contato` virou o dono do lead**. Três buracos fechados de uma vez, todos vistos na bateria: (1) **3 de 6 contatos escalados não existiam em `leads_lead`** — inclusive quem disse "quero garantir minha vaga" —, porque a regra de handoff manda parar de qualificar e a MAG parava antes de registrar; agora `escalar_contato` garante o `Lead` por WhatsApp mesmo **sem nome e sem curso** (`garantir_lead()`, extraída de `LeadPublicoSerializer.create` pros dois caminhos passarem pelo mesmo dedup), com `status=em_atendimento` e `curso_slug` opcional resolvido pelo `resolver_curso()` tolerante da 023. (2) **`ContatoEscalado` deixou de ser "existe = silenciado"**: ganhou `resolvido_em` e `expira_em`, o `unique` do `numero` **caiu** (o mesmo contato pode ser escalado de novo meses depois sem colidir com o registro antigo), e `identificar_contato`/`processar_nutridora` passaram a olhar só os **ativos** — antes, ser escalado uma vez tirava o lead da régua da Nutridora pra sempre. (3) **Silêncio virou cortesia**: contato escalado que insiste recebe **uma** mensagem fixa (sem LLM, sem promessa de prazo), controlada por chave Redis `mag:cortesia:{numero}` com TTL de 24h — repetir a cada mensagem seria pior que o silêncio. *Porquê:* com 6 de 6 conversas terminando em handoff, ele deixou de ser exceção e virou o fluxo; quem cai fora da tabela de leads some do Radar e da Nutridora, e não há segunda rede embaixo. **Contrapartida aceita:** leads magros (só WhatsApp) na base — lead sem nome ainda é um número pra ligar, e ele já está silenciado pro automático, então não vira spam. **Prazo de 24h configurável em `ConfiguracaoSite.handoff_expira_horas`** (`0` = nunca), errando de propósito pro lado de voltar a atender: o pior caso hoje é silêncio infinito, e o pior caso com prazo é a MAG falar com alguém que o gestor já atende — visível e recuperável. **Canal único:** o texto do aviso ao gestor deixou de ser 100% redigido pela IA — o `mag-avisar-equipe.json` anexa o número do contato em linha própria e a instrução de responder pelo WhatsApp da Magma. O número vem do **contexto da execução** (`valueProvider: fieldValue`), não redigitado pelo modelo, pelo mesmo motivo da 023. Fica registrado que isso é **orientação, não trava** — nada impede o gestor de responder do celular dele; trava real exigiria caixa de entrada compartilhada, que é outro produto.
- **2026-07-25 (achado ao implementar a 025)** · **A Nutridora nunca alcança lead nascido no WhatsApp**, porque `processar_nutridora` exclui `utm_source="whatsapp"`. Faz sentido na origem (quem está conversando com a MAG agora não precisa de toque agendado), mas **invalida a decisão tomada na spec 028** de que "quem recebeu o link de matrícula e não preencheu fica com a Nutridora, zero código novo": essa pessoa é lead de WhatsApp por definição, então não receberia nada. Registrado como 028-T20, com a proposta de abrir uma exceção estreita (lead com link enviado e sem `Matrícula` entra na régua mesmo sendo de WhatsApp) em vez de mexer na exclusão geral. *Porquê registrar:* é o segundo caso na mesma semana em que uma decisão de produto parecia "zero código" e o código dizia o contrário — o primeiro foi o timeout do nó de agente na 027, que não existe. Antes de fechar decisão que depende de comportamento existente, **ler o código que implementa esse comportamento**.
- **2026-07-25** · **O harness de simulação de conversa virou parte do repo** (`plataforma/n8n/simulacao/`). Ele viveu duas sessões num diretório temporário e **foi apagado no meio da implementação da 025**, obrigando a reconstruí-lo na hora. *Porquê:* três tarefas dependem dele (024-T11, 025-T12/T13, 027-T1) e o que ele carrega não é código trivial — é conhecimento que custou uma sessão inteira pra descobrir: o formato do payload `messages.upsert` da Evolution, que o `execution_data` do n8n é serializado com `flatted` (e por isso `JSON.parse` não abre), onde mora o `sqlite3` dentro do pnpm store do container, e o truque dos números com **DDD 00** — que não existem em WhatsApp real e por isso fazem o nó de envio falhar de propósito, garantindo que nenhuma bateria de teste entregue mensagem ao telefone de alguém (a instância Evolution de dev está conectada de verdade).
- **2026-07-25** · **A Nutridora passa a excluir por ATIVIDADE, não por origem** (028-T20, decisão do Daniel). A regra `.exclude(utm_source="whatsapp")` em `processar_nutridora` será removida e trocada por uma exclusão de quem teve conversa nos últimos `ConfiguracaoSite.nutridora_silencio_dias` (padrão 2), lida de `apps.conversas`. *Porquê:* a intenção original da regra era "quem está falando com a MAG agora não precisa de toque agendado" — mas ela usava a **origem** como proxy da **atividade**, e origem não muda nunca. Como o `registrar_lead` do SDR carimba `utm_source="whatsapp"` fixo no workflow, o efeito real era: **nenhum lead nascido de conversa com a MAG entra na régua T+1/3/7, para sempre** — e a campanha do Meta é Click-to-WhatsApp, então 100% do lead pago cairia nessa faixa. A exceção estreita proposta antes (só quem recebeu link de matrícula) foi descartada por resolver o sintoma da spec 028 e deixar o furo do funil aberto. Só é possível agora porque a spec 021 criou `Conversa.ultima_atividade_em` — não existia quando a regra foi escrita. **Efeito colateral aceito:** para lead de WhatsApp a régua passa a contar da última conversa, não da criação.
- **2026-07-25** · **Ordem de execução acordada com o Daniel, registrada em `.context/roteiro.md`**: (1) T20, (2) promover 021→025 pra produção, (3) specs 026 e 028, (4) investigar o travamento do n8n (027), (5) pixel e campanha do Meta. *Porquê registrar:* havia cinco frentes prontas ou quase prontas competindo pela mesma sessão, e a escolha entre elas estava sendo refeita a cada abertura de contexto. O `status.md` continua sendo o registro do que aconteceu; o `roteiro.md` é o do que vai acontecer.
- **2026-07-25** · **A cobrança real do Asaas está configurada em produção** (informado pelo Daniel; não verificado a partir da máquina de dev). Sai da lista de pendências da spec 015. *Porquê registrar:* o `status.md` afirmava o contrário desde 23/07, e essa afirmação vinha sendo repetida em cada resumo de sessão.
- **2026-07-25 (incidente)** · **A MAG não atende grupo: identificador que não é telefone de pessoa é recusado em cinco pontos do backend, além de barrado no n8n.** Descoberto durante o deploy: a MAG tinha respondido dentro de **quatro grupos** pelo WhatsApp principal do negócio (`<whatsapp-do-negocio>`) — dois de turma (ofereceu o curso a quem já estava matriculado nele), um da família, e o `grupo de turma 3`, onde tratou o anúncio de inscrição do instrutor como se fosse um lead, respondeu 4× na frente do grupo e acionou a equipe. *Causa:* `remoteJid.split('@')[0]` no nó `Extrair dados` — em grupo isso devolve o **id do grupo**, que passa por número sem levantar suspeita (só dígitos, sem pontuação). *Decisão:* `numero_de_pessoa()` (`apps/nucleo/numeros.py`) — só dígitos, 10 a 15 caracteres — aplicada em `identificar_contato` (400 em vez de "desconhecido", derrubando a execução antes de qualquer texto sair), `escalar_contato`, `garantir_lead`/`validate_whatsapp`, `processar_nutridora` e `listar_gestores`; mais a condição `remoteJid endsWith "@s.whatsapp.net"` no n8n, como **lista de permissão** (grupo, canal e status já nascem de fora, e um tipo de JID novo do WhatsApp também). *Porquê a checagem ser burra de propósito:* não tenta validar DDD nem operadora — o objetivo não é provar que o número existe (só o WhatsApp sabe), é recusar o que comprovadamente **não é pessoa**. Errar aceitando um telefone estranho é barato; errar mandando mensagem da empresa pra dentro de um grupo já custou caro uma vez. *Porquê nas duas camadas:* é a terceira vez na mesma semana que a resposta certa é "o n8n filtra por economia, o backend garante por invariante" — ver a spec 023 e a 025.
- **2026-07-25** · **A MAG ignora grupo por completo, em vez de aprender a conversar em grupo.** Ela não tem nenhuma noção de "vários interlocutores": tudo que chega é tratado como uma conversa 1:1 com quem falou por último. Atuar num grupo de turma pode fazer sentido um dia, mas é feature própria — quem ela responde, quando se cala, como não vira ruído — e não sai de graça de um ajuste de filtro. *Porquê registrar:* pra ninguém "reabrir grupos" achando que basta tirar a condição do IF.
